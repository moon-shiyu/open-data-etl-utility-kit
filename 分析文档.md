# 标准 ETL 启动链路 — 代码解析文档

> 本文档基于 `ETL/Setup-Script.sh`、`ETL/ETL_Setup.sh`、`ETL/Standard_ETL.sh` 以及关联的
> `ETL/Standard_ETL.kjb`、`ETL/ETL_Template.ktr` 进行逐层解析。

---

## 1. 完整执行链路

### 1.1 调用关系总览

```
用户 / crontab
  │
  ▼
Setup-Script.sh          ← 每个 ETL 的独立入口脚本
  │
  ├─► . ETL_Setup.sh     ← source：初始化环境变量（NOW / PATH / ETL_ROOT_DIR）
  │
  ├─► 设置业务参数       ← ETL_NAME / ETL_DIR_RELATIVE / DATA_SYNC_ETL
  │
  └─► . Standard_ETL.sh  ← source：接收三个参数，调用 kitchen.sh
        │
        ▼
      kitchen.sh          ← Pentaho DI (Kettle) 命令行引擎
        │
        ▼
      Standard_ETL.kjb    ← 编排 Job：变量设置 → KTR 执行 → 邮件通知
```

### 1.2 逐文件详细说明

#### ① `ETL/Setup-Script.sh` — 每个 ETL 的入口脚本（第 1–9 行）

```bash
. /path/to/directory/open-data-etl-utility-kit/ETL/ETL_Setup.sh

ETL_NAME=Name_of_KTR_file_no_extension
ETL_DIR_RELATIVE=Directory_containing_KTR_file/
DATA_SYNC_ETL=Y

echo "ETL $ETL_NAME has started at $NOW."

. $ETL_ROOT_DIR/Standard_ETL.sh $ETL_NAME $ETL_DIR_RELATIVE $DATA_SYNC_ETL
```

| 行为 | 说明 |
|------|------|
| **第 1 行** `.` (source) | 加载 `ETL_Setup.sh`，在当前 shell 中设置 `NOW`、`PATH`、`ETL_ROOT_DIR` 三个环境变量 |
| **第 3 行** `ETL_NAME` | KTR 转换文件名（不含 `.ktr` 后缀），如 `Crimes_2001_to_present-ijzp-q8t2` |
| **第 4 行** `ETL_DIR_RELATIVE` | KTR 文件所在目录相对于 `ETL_ROOT_DIR` 的路径，以 `/` 结尾 |
| **第 5 行** `DATA_SYNC_ETL` | 是否启用 DataSync 流程：`Y` 启用 / `N` 不启用，影响后续邮件内容 |
| **第 7 行** `echo` | 在控制台打印启动日志（`$NOW` 来自 `ETL_Setup.sh`） |
| **第 9 行** `.` (source) | 加载 `Standard_ETL.sh` 并传入三个位置参数 |

#### ② `ETL/ETL_Setup.sh` — 公共环境初始化（第 1–3 行）

```bash
NOW=$(date +"%Y%m%d_%H%M%S")
PATH=$PATH:/path/to/jdk/bin
ETL_ROOT_DIR=/path/to/directory/open-data-etl-utility-kit/ETL
```

| 变量 | 说明 |
|------|------|
| `NOW` | 格式 `YYYYMMDD_HHmmss` 的时间戳，用于日志文件名去重 |
| `PATH` | 追加 JDK bin 目录，确保 `java` 可被 Kettle 的 `kitchen.sh` 调用 |
| `ETL_ROOT_DIR` | ETL 框架根目录的绝对路径，所有后续脚本和 KTR/KJB 的基址 |

> **注意**：路径 `/path/to/...` 为占位符，部署时需替换为实际安装路径。

#### ③ `ETL/Standard_ETL.sh` — 调用 kitchen.sh（第 1–7 行）

```bash
ETL_NAME=$1
ETL_DIR_RELATIVE=$2
DATA_SYNC_ETL=$3

../data-integration/kitchen.sh \
  -file="$ETL_ROOT_DIR/Standard_ETL.kjb" \
  -param:P_ETL_Transformation_Directory=$ETL_DIR_RELATIVE \
  -param:P_ETL_Transformation_Name=$ETL_NAME \
  -param:P_Data_Sync_ETL=$DATA_SYNC_ETL \
  -level=Detailed \
  > "../Log/${ETL_NAME}_${NOW}.txt"
```

| 参数 / 元素 | 说明 |
|------|------|
| `$1` → `ETL_NAME` | 从位置参数接收，KTR 文件名（无后缀） |
| `$2` → `ETL_DIR_RELATIVE` | 从位置参数接收，KTR 的相对目录 |
| `$3` → `DATA_SYNC_ETL` | 从位置参数接收，DataSync 开关 |
| `../data-integration/kitchen.sh` | Kettle 的命令行 Job 执行器，位于 ETL 根目录的上一级 |
| `-file` | 指向编排 Job 文件 `Standard_ETL.kjb` |
| `-param:P_ETL_Transformation_Directory` | 传给 KJB 的参数：KTR 所在相对目录 |
| `-param:P_ETL_Transformation_Name` | 传给 KJB 的参数：KTR 文件名 |
| `-param:P_Data_Sync_ETL` | 传给 KJB 的参数：是否 DataSync ETL |
| `-level=Detailed` | Kettle 日志级别（Detailed 为较详细的调试级日志） |
| `> ../Log/${ETL_NAME}_${NOW}.txt` | 标准输出重定向到日志文件；`${ETL_NAME}` 和 `${NOW}` 用花括号隔离以避免变量名歧义 |

#### ④ `ETL/Standard_ETL.kjb` — Kettle Job 编排流程

Job 接收三个参数后，按以下 DAG 顺序执行：

```
START
  │
  ▼
Set variables                ← 将 Job 参数映射为 JVM 级变量
  │                            Data_Sync_ETL, ETL_Transformation_Name, ETL_Specific_To_Addresses
  ▼
ETL_Specific_To_Addresses    ← 运行 Utilities/ETL_Specific_To_Addresses.ktr
  │                            解析当前 ETL 的邮件收件人列表
  ▼
Run KTR                      ← 执行实际业务转换
  │                            路径: ${ETL_DIRECTORY}${P_ETL_Transformation_Directory}${P_ETL_Transformation_Name}.ktr
  ├─ 成功 (TRUE) ──► ETL_Completion_E-Mail - TRUE   ← 发送"执行成功"通知邮件
  └─ 失败 (FALSE) ─► ETL_Completion_E-Mail - FALSE  ← 发送"执行失败"告警邮件
```

**关键 KJB 条目说明**：

| Job Entry | 类型 | 作用 |
|-----------|------|------|
| `Set variables` | `SET_VARIABLES` | 把 `P_Data_Sync_ETL`、`P_ETL_Transformation_Name` 等参数设为 `CURRENT_JOB` 级别变量，供后续步骤引用 |
| `ETL_Specific_To_Addresses` | `TRANS` | 调用 `Utilities/ETL_Specific_To_Addresses.ktr`，根据 ETL 名称确定邮件接收地址 |
| `Run KTR` | `TRANS` | 核心步骤——加载并运行 `${ETL_DIRECTORY}${P_ETL_Transformation_Directory}${P_ETL_Transformation_Name}.ktr` |
| `ETL_Completion_E-Mail - TRUE` | `TRANS` | 调用 `Utilities/ETL_Completion_E-Mail.ktr`，`P_Body_Start` 为成功消息体 |
| `ETL_Completion_E-Mail - FALSE` | `TRANS` | 调用同一 KTR，`P_Body_Start` 为失败消息体 |

### 1.3 KTR 模板内部流程（ETL_Template.ktr）

业务 KTR（由 `ETL_Template.ktr` 复制而来）的执行顺序：

```
Table input               ← SQL 查询提取源数据
  │
  ▼
Get Variables             ← 读取 P_File / P_DatasetID / P_ControlFile 等参数
  │
  ├──────────────────────┐
  ▼                      ▼
Text file output    Prepare_for_DataSyncSSync (sub-transformation)
  │                      │
  │                      ▼
  │               Wait for text file (BlockUntilStepsFinish)
  │                      │
  │                      ▼
  │               DataSync HTTP (sub-transformation)
  │                      │
  │                      ▼
  │               DSResult (JavaScript 解析返回)
  │                      │
  │                      ▼
  │               Copy rows to result
  │
  └─► (两条并行分支最终汇合)
```

- **左分支**：将查询结果写入 CSV 文件（由 `P_File` 指定文件名）。
- **右分支**：等待文件写入完成后，调用 `Utilities/DataSyncHTTP.ktr` 子转换，通过 HTTP 方式将数据发布到 Socrata 平台。
- `DSResult` 步骤用 JavaScript 拼接 DataSync 返回的结果、错误和退出码。

---

## 2. 关键配置与参数传递链路

### 2.1 参数传递链

```
Setup-Script.sh (硬编码)
  │ ETL_NAME=xxx, ETL_DIR_RELATIVE=xxx/, DATA_SYNC_ETL=Y
  │
  ▼
Standard_ETL.sh (位置参数 $1/$2/$3)
  │
  ▼
kitchen.sh -param:P_ETL_Transformation_Name=...
           -param:P_ETL_Transformation_Directory=...
           -param:P_Data_Sync_ETL=...
  │
  ▼
Standard_ETL.kjb (Job 参数声明)
  │ P_ETL_Transformation_Name
  │ P_ETL_Transformation_Directory
  │ P_Data_Sync_ETL
  │
  ▼
Set variables (映射为 JVM 变量)
  │ Data_Sync_ETL = ${P_Data_Sync_ETL}
  │ ETL_Transformation_Name = ${P_ETL_Transformation_Name}
  │
  ▼
Run KTR → ${ETL_DIRECTORY}${P_ETL_Transformation_Directory}${P_ETL_Transformation_Name}.ktr
  │
  ▼
ETL_Template.ktr (KTR 内部参数)
  │ P_ControlFile — DataSync JSON 控制文件名
  │ P_DatasetID   — Socrata 数据集 4x4 ID
  │ P_File        — 输出 CSV 文件名
```

### 2.2 日志路径

| 日志 | 路径 | 生成者 |
|------|------|--------|
| kitchen.sh 运行日志 | `../Log/${ETL_NAME}_${NOW}.txt` | `Standard_ETL.sh` 的 stdout 重定向 |
| 控制台启动消息 | stdout: `ETL $ETL_NAME has started at $NOW.` | `Setup-Script.sh` 的 `echo` |

> 假设 ETL 框架部署在 `/opt/open-data-etl-utility-kit/ETL/`，则日志写入 `/opt/open-data-etl-utility-kit/Log/`。

### 2.3 DataSync 控制文件

每个 ETL 目录下有一个 `*_control.json`（模板为 `ETL/_control.json`），定义了：

| 字段 | 说明 |
|------|------|
| `action` | 数据操作类型：`Replace`（全量替换）等 |
| `csv.columns` | CSV 列名列表，对应 Socrata API field name |
| `csv.separator` | 分隔符，默认 `,` |
| `csv.encoding` | 编码，默认 `utf-8` |

---

## 3. 静态验证方法（无 Kettle / DataSync 环境）

在本地缺少 Pentaho DI（`kitchen.sh`）和 DataSync（`datasync.jar`）的情况下，可对以下内容做静态验证：

### 3.1 Shell 脚本语法验证

```bash
# 对所有 .sh 文件做 bash 语法检查（不执行）
bash -n ETL/Setup-Script.sh
bash -n ETL/ETL_Setup.sh
bash -n ETL/Standard_ETL.sh
bash -n ETL/Utilities/*.sh    # 如有
bash -n Log/*.sh
bash -n DataSync/*.sh
```

`bash -n` 只解析语法结构，不执行任何命令，可检出：
- 引号不匹配
- 未闭合的 `if/then/fi`、`do/done`
- 变量替换语法错误

### 3.2 Shell 脚本静态分析（推荐工具）

```bash
# ShellCheck — 深度静态分析
shellcheck ETL/Setup-Script.sh
shellcheck ETL/ETL_Setup.sh
shellcheck ETL/Standard_ETL.sh
```

ShellCheck 可额外检出：
- 未加引号的变量展开（如 `$ETL_NAME` 应写为 `"$ETL_NAME"`）
- 潜在的 word splitting / globbing 问题
- 废弃语法和不推荐的写法

### 3.3 XML 结构验证（KJB / KTR 文件）

```bash
# 使用 xmllint 检查 XML 是否 well-formed
xmllint --noout ETL/Standard_ETL.kjb
xmllint --noout ETL/ETL_Template.ktr
xmllint --noout ETL/Utilities/*.ktr
```

可进一步使用 XPath 验证关键元素存在性：

```bash
# 检查 KJB 中是否包含必要的 Job Entry
xmllint --xpath '//entry/name' ETL/Standard_ETL.kjb

# 检查 KTR 中是否包含必要的步骤
xmllint --xpath '//step/name' ETL/ETL_Template.ktr

# 检查 KJB 参数声明
xmllint --xpath '//parameter/name' ETL/Standard_ETL.kjb
```

### 3.4 JSON 控制文件验证

```bash
# 验证 JSON 语法（注意：_control.json 模板中有 // 注释，需先去除）
# 对实际使用的控制文件：
python3 -m json.tool ETL/Data_Set_Name_abcd-1234/Data_Set_Name_control.json > /dev/null

# 或使用 jq：
jq . ETL/Data_Set_Name_abcd-1234/Data_Set_Name_control.json > /dev/null
```

> **注意**：模板文件 `ETL/_control.json` 包含 `//` 行内注释（第 16 行），这不是合法 JSON。验证前需去除注释行或对已实例化的控制文件做验证。

### 3.5 参数一致性验证（脚本方式）

可编写简单脚本交叉检查参数名的一致性：

```bash
#!/bin/bash
# 检查 Setup-Script.sh 中的 ETL_NAME 对应的 KTR 文件是否存在
source_script="ETL/Setup-Script.sh"
etl_name=$(grep '^ETL_NAME=' "$source_script" | cut -d= -f2)
etl_dir=$(grep '^ETL_DIR_RELATIVE=' "$source_script" | cut -d= -f2)
ktr_path="ETL/${etl_dir}${etl_name}.ktr"

if [ -f "$ktr_path" ]; then
  echo "OK: $ktr_path exists"
else
  echo "ERROR: $ktr_path not found"
fi
```

### 3.6 日志目录验证

```bash
# 确保日志目录存在且有写权限
test -d ../Log && test -w ../Log && echo "Log dir OK" || echo "Log dir issue"
```

---

## 4. 外部环境依赖

以下组件无法在本地模拟，需要完整的部署环境才能真正运行：

### 4.1 必须的外部依赖

| 依赖 | 用途 | 涉及的脚本/文件 |
|------|------|-----------------|
| **Pentaho Data Integration (Kettle)** | 提供 `kitchen.sh`（Job 执行器）和 `pan.sh`（Transformation 执行器） | `Standard_ETL.sh` 调用 `../data-integration/kitchen.sh` |
| **JDK / JRE** | Kettle 是 Java 应用，需要 JVM 运行 | `ETL_Setup.sh` 中 `PATH=$PATH:/path/to/jdk/bin` |
| **Socrata DataSync** | 数据发布工具，通过 HTTP/FTP 将 CSV 上传到 Socrata 开放数据平台 | `ETL_Template.ktr` 中 `DataSync HTTP (sub-transformation)` 调用 `Utilities/DataSyncHTTP.ktr`；`DataSync/load_preferences.sh` 调用 `datasync.jar` |
| **Socrata 平台账户和 API 凭据** | DataSync 需要认证信息（App Token、用户名、密码） | DataSync 配置中隐含 |
| **数据源数据库** | `ETL_Template.ktr` 中 `Table input` 步骤需要数据库连接 | KTR 的 `<connection/>` 配置 |
| **SMTP 邮件服务器** | `ETL_Completion_E-Mail.ktr` 发送执行结果通知 | `Standard_ETL.kjb` 的邮件步骤 |

### 4.2 网络与基础设施依赖

| 依赖 | 说明 |
|------|------|
| **Socrata API 端点** | DataSync HTTP 需要访问 Socrata 数据发布 API（如 `https://data.cityofchicago.org`） |
| **数据库服务器** | Table input 步骤通过 JDBC 连接源数据库 |
| **文件系统权限** | 需要对 `../Log/` 目录有写权限，对 ETL 目录下 KTR/KJB 有读权限 |
| **Kettle Slave Server**（可选） | `ETL_Template.ktr` 中配置了 `10.96.12.40:8080` 作为远程执行服务器 |

### 4.3 依赖关系图

```
本地可验证（静态）                    需要外部环境（运行时）
─────────────────                    ─────────────────────
Shell 脚本语法 (bash -n)             kitchen.sh (Pentaho DI)
Shell 静态分析 (shellcheck)          Java / JDK
XML well-formed (xmllint)           datasync.jar (Socrata DataSync)
JSON 语法 (jq / python)             Socrata API 端点
参数一致性 (grep + 文件检查)          源数据库 (JDBC)
                                    SMTP 邮件服务器
                                    Kettle Slave Server (可选)
                                    文件系统写权限 (Log/)
```

---

## 5. 总结

本框架采用 **三层 Shell 脚本 + Kettle Job/Transformation** 的分层架构：

1. **`Setup-Script.sh`**（入口层）：每个 ETL 一个，负责 source 公共环境、设置业务参数、调用执行层。
2. **`ETL_Setup.sh` + `Standard_ETL.sh`**（框架层）：公共脚本，负责环境初始化和 kitchen.sh 调用。
3. **`Standard_ETL.kjb`**（编排层）：Kettle Job，负责变量设置、KTR 执行、结果通知的 DAG 编排。
4. **`ETL_Template.ktr`**（业务层）：具体的数据提取、转换和 DataSync 发布逻辑。

这种分层设计使得新增 ETL 只需复制 `Setup-Script.sh` 和 `ETL_Template.ktr`，修改参数和 SQL 即可，无需改动框架代码。
