# Kettle ETL 框架代码解析文档

> 本文档基于对 `ETL/_control.json`、`ETL/ETL_Template.ktr`、`ETL/Standard_ETL.kjb` 和 `docs/creating-configuring-ETL.rst` 及相关辅助文件的完整阅读，覆盖解析结论、关键链路/配置说明、静态验证方法和外部环境依赖。

---

## 一、三大核心文件的关系

### 1.1 总览

整个 ETL 框架由三层结构组成，自上而下分别为 **作业编排层 → 转换执行层 → 数据同步配置层**：

```
┌─────────────────────────────────────────────────────────────────────────┐
│  Standard_ETL.kjb (作业 / Job)                                         │
│  ┌──────────┐  ┌──────────┐  ┌────────────┐  ┌───────────────────────┐ │
│  │  START   │→│ Set vars │→│ Run KTR    │→│ ETL_Completion_E-Mail │ │
│  └──────────┘  └──────────┘  │(调用下面的  │  │  TRUE / FALSE 分支    │ │
│                               │  .ktr 文件)│  └───────────────────────┘ │
│                               └─────┬────┘                             │
└─────────────────────────────────────┼──────────────────────────────────┘
                                      │ 参数传递
                                      ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  ETL_Template.ktr (转换 / Transformation)                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────────────┐   │
│  │Table     │→│Get Vars  │→│Text file │→│Prepare_for_DataSync  │   │
│  │input     │  │(P_File,  │  │output    │  │SSync (子转换)        │   │
│  │          │  │P_Dataset │  │(写CSV)   │  └──────────┬───────────┘   │
│  └──────────┘  │ID等)     │  └──────────┘             │               │
│                └──────────┘                  ┌────────▼───────────┐   │
│                                              │DataSync HTTP(子转换)│   │
│                                              │→ DSResult → Copy   │   │
│                                              │  rows to result    │   │
│                                              └────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
                                      │
                                      │ 引用 _control.json
                                      ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  _control.json (DataSync 控制配置模板)                                   │
│  定义: action(Replace), CSV 格式(分隔符/编码/时间戳), API 列名映射        │
└─────────────────────────────────────────────────────────────────────────┘
```

### 1.2 各文件职责

| 文件 | 类型 | 职责 |
|------|------|------|
| `_control.json` | JSON 配置模板 | DataSync 的控制文件模板，定义 CSV 解析规则（分隔符、编码、时间戳格式、跳过行数）和目标数据集的 API 字段名列表 (`columns`)。每个数据集需从此模板派生自己的控制文件。 |
| `ETL_Template.ktr` | Kettle 转换 (.ktr) | 数据提取与转换的模板。定义了参数化的数据流水线：从数据库读取 → 变量解析 → 写出 CSV → 调用 DataSync 上传至 Socrata 门户。包含 3 个核心参数（P_ControlFile、P_DatasetID、P_File）。 |
| `Standard_ETL.kjb` | Kettle 作业 (.kjb) | 顶层作业编排器。负责设置运行时变量、按路径调用具体的 .ktr 转换、根据转换成功/失败结果发送不同内容的邮件通知。接受 3 个参数（P_ETL_Transformation_Directory、P_ETL_Transformation_Name、P_Data_Sync_ETL）。 |

### 1.3 参数传递链路

```
Standard_ETL.kjb 接收参数:
  P_ETL_Transformation_Directory  ─┐
  P_ETL_Transformation_Name       ─┼─→ 拼接路径: ${ETL_DIRECTORY}${P_ETL_Transformation_Directory}${P_ETL_Transformation_Name}.ktr
  P_Data_Sync_ETL (默认 "N")      ─┘   调用具体的 .ktr 转换

具体 .ktr（从 ETL_Template.ktr 派生）接收参数:
  P_ControlFile  → 解析为: ${Internal.Transformation.Filename.Directory}/${P_ControlFile}
  P_DatasetID    → 解析为: ${P_DatasetID}
  P_File         → 解析为: ${Internal.Transformation.Filename.Directory}/files/${P_File}
```

Standard_ETL.kjb 通过 `<pass_all_parameters>Y</pass_all_parameters>` 将参数透传给子转换。

---

## 二、新增数据集 ETL 所需配置与文件

### 2.1 完整清单

为一个新数据集（假设 4x4 为 `abcd-1234`，数据集名称为 `Data_Set_Name`）创建 ETL，需要准备以下文件：

| # | 文件/操作 | 来源/模板 | 说明 |
|---|----------|----------|------|
| 1 | **目录** `ETL/Data_Set_Name_abcd-1234/` | 新建 | 数据集专属目录，命名格式: `Name_4x4` |
| 2 | **转换文件** `ETL/Data_Set_Name_abcd-1234/Data_Set_Name_abcd-1234.ktr` | 从 `ETL/ETL_Template.ktr` 复制 | 主转换文件，需修改 Table input 步骤的 SQL 查询和数据源连接 |
| 3 | **控制文件** `ETL/Data_Set_Name_abcd-1234/Data_Set_Name_control.json` | 从 `ETL/_control.json` 复制 | DataSync 配置，需填入真实的 API 字段名列表 |
| 4 | **启动脚本** `ETL/Data_Set_Name_abcd-1234/abcd-1234.sh` | 从 `Setup-Script.sh` 复制 | 设置 `ETL_NAME` 和 `ETL_DIR_RELATIVE` 变量 |
| 5 | **邮件地址文件** `ETL/Data_Set_Name_abcd-1234/ETL_Specific_To_Addresses.txt` | 新建（可选） | 该 ETL 专属的额外邮件通知地址（第一行有效） |
| 6 | **files/ 子目录** `ETL/Data_Set_Name_abcd-1234/files/` | 新建 | 存放中间 CSV 文件（由转换自动创建） |
| 7 | **cron 定时任务** | 手动配置 | 在 crontab 中注册启动脚本和日志输出路径 |

### 2.2 操作步骤详解

**步骤 1 — 准备目标端（Socrata 门户）**
- 在 Socrata 门户创建数据集，配置所有列的 API 字段名
- 记录数据集的 4x4 ID（如 `abcd-1234`）

**步骤 2 — 创建目录与复制模板**
```bash
# 创建数据集目录
mkdir ETL/Data_Set_Name_abcd-1234
mkdir ETL/Data_Set_Name_abcd-1234/files

# 复制转换模板
cp ETL/ETL_Template.ktr ETL/Data_Set_Name_abcd-1234/Data_Set_Name_abcd-1234.ktr

# 复制控制文件模板
cp ETL/_control.json ETL/Data_Set_Name_abcd-1234/Data_Set_Name_control.json
```

**步骤 3 — 配置 Kettle 转换参数**

在 Kettle 中打开 .ktr 文件，进入 `Edit → Settings → Parameters`：

| 参数 | 示例值 | 说明 |
|------|--------|------|
| P_ControlFile | `Data_Set_Name_control.json` | 控制文件名 |
| P_DatasetID | `abcd-1234` | 数据集 4x4 ID |
| P_File | `Data_Set_Name_abcd-1234.csv` | 中间输出 CSV 文件名 |

**步骤 4 — 编辑控制文件**

编辑 `Data_Set_Name_control.json`，将 `columns` 数组中的占位符替换为真实 API 字段名：
```json
{
  "action": "Replace",
  "csv": {
    "columns": ["field_1", "field_2", "field_3"],
    "separator": ",",
    "encoding": "utf-8",
    "fixedTimestampFormat": "MM/dd/yyyy",
    ...
  }
}
```

**步骤 5 — 修改数据提取逻辑**

在 .ktr 文件中修改 `Table input` 步骤：
- 配置数据库连接
- 编写实际的 SQL 查询语句（替换占位符 `SELECT <values> FROM <table name> WHERE <conditions>`）
- 可根据需要添加自定义转换步骤

> **重要**: 以下框架步骤不可修改：Get Variables、Prepare_for_DataSyncSSync、Wait for text file、DataSync HTTP、DSResult、Copy rows to result、Text file output。

**步骤 6 — 配置启动脚本与定时任务**
```bash
# 编辑启动脚本 (abcd-1234.sh)
ETL_NAME=Data_Set_Name_abcd-1234
ETL_DIR_RELATIVE=Data_Set_Name_abcd-1234/

# 配置 crontab
# 每天凌晨 2 点执行
0 2 * * * /path/to/ETL/Data_Set_Name_abcd-1234/abcd-1234.sh >> /path/to/Log/Data_Set_Name_abcd-1234/abcd-1234.log 2>&1
```

---

## 三、作业执行完整流程

### 3.1 从 Shell 到邮件通知的全链路

```
cron / 手动执行
    │
    ▼
abcd-1234.sh (启动脚本)
    │  设置 ETL_NAME, ETL_DIR_RELATIVE, DATA_SYNC_ETL
    │  调用 ETL_Setup.sh（设置 PATH, ETL_ROOT_DIR, NOW 时间戳）
    │
    ▼
Standard_ETL.sh (通用作业启动器)
    │  kitchen.sh -file=Standard_ETL.kjb
    │  -param:P_ETL_Transformation_Directory=$ETL_DIR_RELATIVE
    │  -param:P_ETL_Transformation_Name=$ETL_NAME
    │  -param:P_Data_Sync_ETL=$DATA_SYNC_ETL
    │  -level=Detailed
    │  日志输出 → ../Log/${ETL_NAME}_${NOW}.txt
    │
    ▼
Standard_ETL.kjb (Kettle 作业)
    │
    ├─[1] START
    │
    ├─[2] Set variables
    │     Data_Sync_ETL          = ${P_Data_Sync_ETL}
    │     ETL_Transformation_Name = ${P_ETL_Transformation_Name}
    │     ETL_Specific_To_Addresses = (空占位)
    │
    ├─[3] ETL_Specific_To_Addresses (子转换)
    │     读取: ${ETL_DIRECTORY}${P_ETL_Transformation_Directory}ETL_Specific_To_Addresses.txt
    │     设置变量 ETL_Specific_To_Addresses (PARENT_JOB 级别)
    │     注: 文件不存在时 error_ignored=Y，不会中断
    │
    ├─[4] Run KTR (执行具体 ETL 转换)
    │     路径: ${ETL_DIRECTORY}${P_ETL_Transformation_Directory}${P_ETL_Transformation_Name}.ktr
    │     pass_all_parameters=Y
    │     日志级别: Basic
    │
    ├─[5a] 成功 (evaluation=Y) → ETL_Completion_E-Mail - TRUE
    │       P_Body_Start = "returned a status of TRUE, meaning that it ran to completion"
    │
    └─[5b] 失败 (evaluation=N) → ETL_Completion_E-Mail - FALSE
            P_Body_Start = "returned a status of FALSE...likely was not accomplished"
```

### 3.2 邮件通知机制详解

`ETL_Completion_E-Mail.ktr` 的处理流程：

1. **Get rows from result** — 接收上游转换通过 `Copy rows to result` 传回的 DSResult 数据
2. **Detect empty stream** — 处理空流情况
3. **Get Variables** — 读取邮件配置参数（SMTP 服务器、收件人、发件人等）
4. **Check for DSResult** — 判断 DataSync 是否执行：
   - DSResult 为空 → "Data Sync did not run."
   - Data_Sync_ETL ≠ "Y" → "This is not a DataSync ETL."
5. **Get System Info** — 获取前一作业条目的执行结果和系统时间
6. **Set Subject and Body** — 组装邮件标题和正文：
   - 标题格式: `{ETL_Transformation_Name} - {MM/dd/yyyy} - {Success|Failure}`
   - 成功条件: `previous_job_entry_result==true && (DataSyncExitValue==0 || !DataSync ETL)`
   - 正文: Body_Start + DSResult 详情 + More_Status_Information
7. **Add ETL_Specific_To_Addresses** — 将 ETL 专属地址追加到收件人列表
8. **Send Mail** — 通过 SMTP 发送邮件
9. **Write to log** — 将邮件内容写入日志

---

## 四、静态验证清单

以下清单用于在部署前对新增 ETL 进行静态检查，无需实际运行 Kettle 即可发现问题。

### 4.1 参数命名验证

- [ ] **P_ControlFile** — 值必须与数据集目录中的控制文件名完全匹配（区分大小写，含 `.json` 后缀）
- [ ] **P_DatasetID** — 值必须为合法的 4x4 格式（4 个字母数字 + 连字符 + 4 个字母数字，如 `abcd-1234`），且与 Socrata 门户的数据集 UID 一致
- [ ] **P_File** — 值必须为 CSV 文件名（含 `.csv` 后缀），且与 `Text file output` 步骤实际写出的文件名一致
- [ ] **P_ETL_Transformation_Directory** (kjb 层) — 必须以 `/` 结尾（如 `Data_Set_Name_abcd-1234/`），对应从 ETL 根目录到数据集目录的相对路径
- [ ] **P_ETL_Transformation_Name** (kjb 层) — 必须与 .ktr 文件名完全一致（不含 `.ktr` 后缀）
- [ ] **P_Data_Sync_ETL** (kjb 层) — 值为 `Y` 或 `N`（默认 `N`）；为 `Y` 时邮件中会展示 DataSync 执行结果
- [ ] **参数命名一致性** — .ktr 中的 3 个参数名称必须为 `P_ControlFile`、`P_DatasetID`、`P_File`，不可拼写错误或遗漏

### 4.2 目录约定验证

- [ ] **数据集目录命名** — 必须遵循 `Name_of_Dataset_abcd-1234` 格式（名称 + 下划线 + 4x4 ID）
- [ ] **目录位置** — 数据集目录必须直接位于 `ETL/` 下（即 `ETL/Data_Set_Name_abcd-1234/`）
- [ ] **.ktr 文件命名** — 转换文件名必须与目录名一致（如 `Data_Set_Name_abcd-1234.ktr`）
- [ ] **控制文件位置** — 必须在数据集目录下（如 `ETL/Data_Set_Name_abcd-1234/Data_Set_Name_control.json`）
- [ ] **files/ 子目录** — 中间 CSV 输出目录 `files/` 存在或由 `create_parent_folder=Y` 自动创建
- [ ] **ETL_DIRECTORY 环境变量** — `kettle.properties` 中已正确配置 `ETL_DIRECTORY`（含尾部 `/`）
- [ ] **DATA_SYNC_DIRECTORY 环境变量** — `kettle.properties` 中已正确配置 `DATA_SYNC_DIRECTORY`
- [ ] **Utilities/ 引用路径** — ETL_Template.ktr 中的子转换引用路径为 `${ETL_DIRECTORY}Utilities/DataSyncHTTP.ktr` 和 `${ETL_DIRECTORY}Utilities/Prepare_for_DataSyncSSync.ktr`，确保 Utilities 目录存在且文件完整
- [ ] **ETL_Specific_To_Addresses.txt 位置** — 如需要，该文件必须位于数据集目录下（与 .ktr 同级）

### 4.3 DataSync 开关验证

- [ ] **P_Data_Sync_ETL 设置正确** — 使用 DataSync 上传到 Socrata 的 ETL 应设为 `Y`；仅做数据提取/本地处理的 ETL 应设为 `N`
- [ ] **Standard_ETL.sh 第三个参数** — shell 脚本调用 `Standard_ETL.sh` 时第三个参数 `DATA_SYNC_ETL` 与 kjb 层的 `P_Data_Sync_ETL` 值一致
- [ ] **DataSync 子转换存在** — 当 DataSync 开关为 `Y` 时，确认 `${ETL_DIRECTORY}Utilities/DataSyncHTTP.ktr` 和 `${ETL_DIRECTORY}Utilities/Prepare_for_DataSyncSSync.ktr` 文件存在
- [ ] **控制文件 columns 非空** — `_control.json` 的 `columns` 数组已从模板占位符 `["api_name_1","api_name_2","api_name_3"]` 替换为真实字段名
- [ ] **action 类型正确** — 控制文件中的 `action` 字段（如 `Replace`）符合预期的数据同步策略（Replace / Upsert / Append）

### 4.4 邮件通知验证

- [ ] **SMTP 服务器配置** — `ETL_Completion_E-Mail.ktr` 的 `P_SMTP_Server` 参数已从默认值 `smtp.example.com` 改为实际 SMTP 地址
- [ ] **SMTP 端口** — `P_SMTP_Port` 与实际 SMTP 服务端口匹配（默认 25）
- [ ] **发件人地址** — `P_Sender_Address` 已配置为有效地址（默认 `data-portal-bot@example.com`）
- [ ] **发件人名称** — `P_Sender_Name` 已配置（默认 `Data Portal Bot`）
- [ ] **收件人列表** — `P_To_Address` 已配置为有效的逗号分隔邮箱列表（默认 `etl-admin@example.com, data-portal-admin@example.com, cdo@example.com`）
- [ ] **ETL 专属收件人** — 如需额外通知特定人员，确认 `ETL_Specific_To_Addresses.txt` 存在于数据集目录下，且第一行为有效邮箱地址
- [ ] **邮件主题格式** — 标题自动生成为 `{转换名} - {日期} - {Success/Failure}`，确认转换名可读且有辨识度

### 4.5 日志输出位置验证

- [ ] **Log 目录** — `Log/` 目录存在，且启动脚本的日志重定向路径指向此目录
- [ ] **日志文件命名** — 格式为 `../Log/${ETL_NAME}_${NOW}.txt`（如 `Log/Data_Set_Name_abcd-1234_20260615_143000.txt`），`NOW` 由 `ETL_Setup.sh` 生成，格式为 `YYYYMMDD_HHMMSS`
- [ ] **日志级别** — Standard_ETL.sh 默认使用 `-level=Detailed`，kjb 中 Run KTR 步骤为 `Basic`；确认日志级别满足调试/运维需求
- [ ] **crontab 日志重定向** — crontab 行中的 `>>` 重定向路径正确，且日志目录可写
- [ ] **Kettle 内置日志表** — .ktr 和 .kjb 的 `<trans-log-table>` 和 `<job-log-table>` 中的 `<connection/>` 为空，表示日志不写入数据库而是依赖文件输出（符合预期）

### 4.6 框架步骤完整性验证

- [ ] **不可修改的步骤保留** — 确认以下步骤存在于 .ktr 中且未被修改：
  - `Get Variables` (GetVariable)
  - `Prepare_for_DataSyncSSync (sub-transformation)` (Mapping → Prepare_for_DataSyncSSync.ktr)
  - `Wait for text file` (BlockUntilStepsFinish)
  - `DataSync HTTP (sub-transformation)` (Mapping → DataSyncHTTP.ktr)
  - `DSResult` (ScriptValueMod)
  - `Copy rows to result` (RowsToResult)
  - `Text file output` (TextFileOutput)
- [ ] **步骤间 hop 连接完整** — 数据流 hop 链必须为：`Table input → Get Variables → [Text file output / Prepare_for_DataSyncSSync] → Wait for text file → DataSync HTTP → DSResult → Copy rows to result`
- [ ] **字段映射正确** — Get Variables 步骤中的字段映射：
  - `File` → `${Internal.Transformation.Filename.Directory}/files/${P_File}`
  - `DatasetID` → `${P_DatasetID}`
  - `ControlFile` → `${Internal.Transformation.Filename.Directory}/${P_ControlFile}`

---

## 五、外部环境依赖

### 5.1 软件依赖

| 依赖 | 用途 | 配置位置 |
|------|------|---------|
| **Pentaho Kettle (PDI)** | ETL 引擎，执行 .ktr 和 .kjb | `data-integration/` 目录（符号链接到版本目录） |
| **Java JDK** | Kettle 和 DataSync 的运行时 | `ETL_Setup.sh` 中的 `PATH=$PATH:/path/to/jdk/bin` |
| **Socrata DataSync** | 数据同步到 Socrata 门户的工具 | `DATA_SYNC_DIRECTORY` 环境变量指向 `datasync.jar` |
| **cron** | 定时任务调度 | 系统 crontab |
| **SMTP 服务器** | 邮件通知发送 | `ETL_Completion_E-Mail.ktr` 参数 |

### 5.2 环境变量依赖

| 变量名 | 配置位置 | 说明 |
|--------|---------|------|
| `ETL_DIRECTORY` | `kettle.properties` | ETL 框架根目录绝对路径，需以 `/` 结尾 |
| `DATA_SYNC_DIRECTORY` | `kettle.properties` | DataSync 安装目录绝对路径 |
| `ETL_ROOT_DIR` | `ETL_Setup.sh` | 同 ETL_DIRECTORY，供 Shell 脚本使用 |
| `NOW` | `ETL_Setup.sh` 运行时生成 | 时间戳，格式 `YYYYMMDD_HHMMSS` |
| `PATH` | `ETL_Setup.sh` | 需追加 JDK bin 目录 |

### 5.3 网络依赖

| 依赖 | 说明 |
|------|------|
| Socrata 门户 API | DataSync HTTP 子转换需要访问 Socrata 门户进行数据上传 |
| SMTP 服务器 | 邮件通知需要可访问的 SMTP 服务器（默认端口 25） |
| 数据源数据库 | `Table input` 步骤需要数据库连接（通过 Kettle 数据库连接配置） |

### 5.4 文件系统依赖

```
open-data-etl-utility-kit/
├── ETL/
│   ├── _control.json                          # 控制文件模板
│   ├── ETL_Template.ktr                       # 转换模板
│   ├── Standard_ETL.kjb                       # 标准作业
│   ├── Standard_ETL.sh                        # 作业启动器
│   ├── ETL_Setup.sh                           # 环境初始化
│   ├── Utilities/
│   │   ├── DataSyncHTTP.ktr                   # DataSync HTTP 子转换
│   │   ├── DataSyncFTP.ktr                    # DataSync FTP 子转换
│   │   ├── DataSync.ktr                       # DataSync 通用子转换
│   │   ├── Prepare_for_DataSyncSSync.ktr      # SSync 准备子转换
│   │   ├── Prepare_for_DataSyncFTP.ktr        # FTP 准备子转换
│   │   ├── Prepare_for_DataSyncHTTP.ktr       # HTTP 准备子转换
│   │   ├── ETL_Completion_E-Mail.ktr          # 邮件通知转换
│   │   └── ETL_Specific_To_Addresses.ktr      # 专属邮件地址读取
│   └── Data_Set_Name_abcd-1234/               # 数据集目录（每个数据集一个）
│       ├── Data_Set_Name_abcd-1234.ktr        # 具体转换
│       ├── Data_Set_Name_control.json          # 具体控制文件
│       ├── abcd-1234.sh                        # 启动脚本
│       ├── ETL_Specific_To_Addresses.txt       # 可选：专属邮件地址
│       └── files/                              # 中间 CSV 输出目录
├── Log/                                        # 日志输出目录
├── DataSync/                                   # DataSync 安装与配置
├── Tools/                                      # 管理工具
├── data-integration -> data-integration-x.y.z  # Kettle 符号链接
└── data-integration-x.y.z/                     # Kettle 实际安装
```

---

## 六、关键发现与注意事项

1. **`_control.json` 中包含 JSON 注释**：第 16 行有 `// Edit this line to include the real set of api_name`，这是非标准 JSON 语法。复制后编辑时需删除注释，否则可能导致 DataSync 解析失败。

2. **slave server 硬编码**：`ETL_Template.ktr` 和 `ETL_Completion_E-Mail.ktr` 中均硬编码了 slave server 信息（IP `10.96.12.40`、端口 `8080`、用户名 `jlevy`、加密密码）。生产部署前需根据实际环境修改或清除。

3. **ETL_Specific_To_Addresses.txt 的容错设计**：`ETL_Specific_To_Addresses.ktr` 配置了 `error_ignored=Y`，当文件不存在时不会中断作业执行，而是使用空字符串作为默认值。邮件发送步骤通过 `len([ETL_Specific_To_Addresses])<5` 判断是否需要追加。

4. **DataSync 开关的双重控制**：`P_Data_Sync_ETL` 参数不仅影响邮件内容（是否展示 DataSync 结果），还在 `Check for DSResult` JavaScript 步骤中决定 DSResult 的显示值。当值为非 `Y` 时，DSResult 被覆盖为 "This is not a DataSync ETL."。

5. **More_Status_Information 扩展机制**：邮件正文支持通过全局变量 `More_Status_Information` 追加自定义状态信息。任何上游转换可通过 JavaScript 步骤 `setVariable("More_Status_Information", text, "g")` 设置，邮件发送后自动清空。

---

*文档生成日期: 2026-06-15*
