# ETL 标准启动链路代码解析

## 一、文件概览

| 文件 | 类型 | 职责 |
|------|------|------|
| `ETL/ETL_Setup.sh` | Shell | 全局环境初始化（时间戳、JDK PATH、根目录） |
| `ETL/Setup-Script.sh` | Shell（模板） | 单个 ETL 任务的配置入口，设置任务参数后调用标准流程 |
| `ETL/Standard_ETL.sh` | Shell | 标准执行器，将参数转发给 Pentaho kitchen.sh |
| `ETL/Standard_ETL.kjb` | Kettle Job XML | Pentaho 作业定义，编排变量设置、KTR 转换执行和邮件通知 |
| `ETL/_control.json` | JSON | DataSync 控制文件，定义 CSV 数据同步的列映射和格式 |

---

## 二、完整执行链路

### 2.1 调用关系总览

```
[cron / 手动触发]
       |
       v
 Setup-Script.sh        （第 1 层：任务入口）
       |
       |--- source ---> ETL_Setup.sh       （第 2 层：加载全局环境）
       |
       |--- 设置 ETL_NAME / ETL_DIR_RELATIVE / DATA_SYNC_ETL
       |
       |--- source ---> Standard_ETL.sh    （第 3 层：标准执行器）
                              |
                              v
                         kitchen.sh         （第 4 层：Pentaho CLI）
                              |
                              v
                         Standard_ETL.kjb   （第 5 层：Kettle 作业编排）
                              |
                    +---------+---------+
                    |                   |
                    v                   v
              Run KTR (成功)      Run KTR (失败)
                    |                   |
                    v                   v
           邮件通知(TRUE)        邮件通知(FALSE)
```

### 2.2 逐步详解

#### 第 1 步：ETL_Setup.sh — 全局环境初始化

```bash
# ETL/ETL_Setup.sh（共 3 行）
NOW=$(date +"%Y%m%d_%H%M%S")                                    # 生成时间戳，用于日志文件名
PATH=$PATH:/path/to/jdk/bin                                      # 将 JDK 加入 PATH（kitchen.sh 依赖 Java）
ETL_ROOT_DIR=/path/to/directory/open-data-etl-utility-kit/ETL    # 设置 ETL 工程根目录
```

**关键变量：**

| 变量 | 作用 | 示例值 |
|------|------|--------|
| `NOW` | 时间戳，拼入日志文件名 | `20260615_143022` |
| `PATH` | 确保 `java` 命令可用 | 追加 JDK bin 目录 |
| `ETL_ROOT_DIR` | 所有后续路径的基准 | `/path/to/directory/open-data-etl-utility-kit/ETL` |

#### 第 2 步：Setup-Script.sh — 任务参数配置

```bash
# ETL/Setup-Script.sh
. /path/to/directory/open-data-etl-utility-kit/ETL/ETL_Setup.sh   # (1) 加载环境

ETL_NAME=Name_of_KTR_file_no_extension       # (2) KTR 转换文件名（不含 .ktr 后缀）
ETL_DIR_RELATIVE=Directory_containing_KTR_file/  # (3) KTR 所在子目录的相对路径（以 / 结尾）
DATA_SYNC_ETL=Y                               # (4) 是否为 DataSync 类型 ETL（Y/N）

echo "ETL $ETL_NAME has started at $NOW."     # (5) 打印启动日志

. $ETL_ROOT_DIR/Standard_ETL.sh $ETL_NAME $ETL_DIR_RELATIVE $DATA_SYNC_ETL  # (6) 调用标准执行器
```

> **注意**：此文件是一个 **模板**，实际使用时需复制此文件并替换 `ETL_NAME`、`ETL_DIR_RELATIVE`、`DATA_SYNC_ETL` 为真实值。例如，若有一个位于 `ETL/MyProject/` 下名为 `load_users.ktr` 的转换文件，则应设置 `ETL_NAME=load_users`，`ETL_DIR_RELATIVE=MyProject/`。

#### 第 3 步：Standard_ETL.sh — 调用 Pentaho Kitchen

```bash
# ETL/Standard_ETL.sh
ETL_NAME=$1               # 从位置参数接收
ETL_DIR_RELATIVE=$2
DATA_SYNC_ETL=$3

../data-integration/kitchen.sh \
  -file="$ETL_ROOT_DIR/Standard_ETL.kjb" \
  -param:P_ETL_Transformation_Directory=$ETL_DIR_RELATIVE \
  -param:P_ETL_Transformation_Name=$ETL_NAME \
  -param:P_Data_Sync_ETL=$DATA_SYNC_ETL \
  -level=Detailed \
  > "../Log/${ETL_NAME}_${NOW}.txt"
```

**参数映射关系：**

| Shell 变量 | kitchen.sh 参数 | KJB 参数名 | 用途 |
|------------|----------------|-----------|------|
| `$ETL_DIR_RELATIVE` | `-param:P_ETL_Transformation_Directory` | `P_ETL_Transformation_Directory` | KTR 文件所在目录（相对路径） |
| `$ETL_NAME` | `-param:P_ETL_Transformation_Name` | `P_ETL_Transformation_Name` | KTR 文件名（无后缀） |
| `$DATA_SYNC_ETL` | `-param:P_Data_Sync_ETL` | `P_Data_Sync_ETL` | DataSync 标志（默认 N） |

**日志输出路径：** `../Log/${ETL_NAME}_${NOW}.txt`
- 相对于 `ETL/` 目录，日志写入上一级的 `Log/` 目录
- 文件名格式：`<ETL名称>_<时间戳>.txt`，例如 `load_users_20260615_143022.txt`

#### 第 4 步：Standard_ETL.kjb — Kettle 作业编排

KJB 文件定义了 Pentaho 作业的执行流程：

```
START
  │ (unconditional)
  v
Set variables                    ← 将参数映射到作业级变量
  │ (unconditional)
  v
ETL_Specific_To_Addresses        ← 加载 ETL 专属邮件收件人（Utilities/ETL_Specific_To_Addresses.ktr）
  │ (unconditional)
  v
Run KTR                          ← 执行实际转换：${ETL_DIRECTORY}${P_ETL_Transformation_Directory}${P_ETL_Transformation_Name}.ktr
  │
  ├── evaluation=Y (成功) ──> ETL_Completion_E-Mail - TRUE   ← 发送成功通知邮件
  │
  └── evaluation=N (失败) ──> ETL_Completion_E-Mail - FALSE  ← 发送失败告警邮件
```

**KJB 内部变量设置（Set variables 步骤）：**

| 变量名 | 值来源 | 作用域 |
|--------|--------|--------|
| `Data_Sync_ETL` | `${P_Data_Sync_ETL}` | CURRENT_JOB |
| `ETL_Transformation_Name` | `${P_ETL_Transformation_Name}` | CURRENT_JOB |
| `ETL_Specific_To_Addresses` | `$[20]`（占位符） | CURRENT_JOB |

**KTR 文件定位公式：**
```
${ETL_DIRECTORY} + ${P_ETL_Transformation_Directory} + ${P_ETL_Transformation_Name} + .ktr
```

---

## 三、关键配置与文件依赖

### 3.1 目录结构要求

```
open-data-etl-utility-kit/
├── data-integration/            ← Pentaho Data Integration（PDI / Kettle）安装目录
│   └── kitchen.sh               ← PDI 命令行工具（执行 .kjb 作业）
├── Log/                         ← 日志输出目录（需预先创建）
│   └── <ETL_NAME>_<timestamp>.txt
└── ETL/
    ├── ETL_Setup.sh             ← 全局环境配置
    ├── Setup-Script.sh          ← 任务模板
    ├── Standard_ETL.sh          ← 标准执行器
    ├── Standard_ETL.kjb         ← Kettle 作业定义
    ├── ETL_Template.ktr         ← KTR 转换模板
    ├── _control.json            ← DataSync 控制文件
    └── Utilities/
        ├── DataSync.ktr                  ← 数据同步转换
        ├── DataSyncFTP.ktr               ← FTP 数据同步
        ├── DataSyncHTTP.ktr              ← HTTP 数据同步
        ├── ETL_Completion_E-Mail.ktr     ← 完成通知邮件转换
        ├── ETL_Specific_To_Addresses.ktr ← 收件人地址查询转换
        ├── Prepare_for_DataSyncFTP.ktr   ← FTP 同步前置准备
        ├── Prepare_for_DataSyncHTTP.ktr  ← HTTP 同步前置准备
        └── Prepare_for_DataSyncSSync.ktr ← SSync 同步前置准备
```

### 3.2 外部环境依赖

| 依赖项 | 路径/位置 | 用途 |
|--------|-----------|------|
| **JDK** | `/path/to/jdk/bin` | Kettle 运行时依赖 Java |
| **Pentaho Data Integration** | `../data-integration/kitchen.sh` | 作业执行引擎 |
| **SMTP 邮件服务** | KTR 内部配置 | 发送 ETL 完成/失败通知 |
| **DataSync 目标平台** | `_control.json` 中配置 | 数据发布端点（如 Socrata 平台） |
| **Log 目录** | `../Log/` | 日志输出目标，需可写 |

---

## 四、静态验证方法（无需 Kettle/DataSync 环境）

在本地没有安装 Pentaho Data Integration 或 DataSync 目标平台的情况下，可以进行以下静态验证：

### 4.1 Shell 脚本语法检查

```bash
# 使用 bash -n 进行语法检查（不实际执行）
bash -n ETL/ETL_Setup.sh
bash -n ETL/Setup-Script.sh
bash -n ETL/Standard_ETL.sh
```

> `bash -n` 只做语法解析，不执行命令，可验证括号匹配、引号闭合、语法结构等问题。

### 4.2 ShellCheck 静态分析

```bash
# 使用 ShellCheck 进行更深层的静态分析
shellcheck ETL/ETL_Setup.sh
shellcheck ETL/Setup-Script.sh
shellcheck ETL/Standard_ETL.sh
```

ShellCheck 可检测：
- 未加引号的变量引用（如 `$ETL_DIR_RELATIVE` 中可能含空格）
- source 路径的硬编码问题
- 变量未定义就使用的风险

### 4.3 KJB/KTR XML 格式验证

```bash
# 验证 XML 格式是否合法
xmllint --noout ETL/Standard_ETL.kjb
xmllint --noout ETL/ETL_Template.ktr
xmllint --noout ETL/Utilities/*.ktr
```

### 4.4 参数传递链一致性验证

可编写脚本检查参数在各层之间的传递是否一致：

```bash
# 检查 Standard_ETL.sh 传递的参数名是否与 KJB 中声明的参数名匹配
grep -oP 'param:\K[^=]+' ETL/Standard_ETL.sh | sort > /tmp/sh_params.txt
grep -oP '<name>\K[^<]+' ETL/Standard_ETL.kjb | head -3 | sort > /tmp/kjb_params.txt
diff /tmp/sh_params.txt /tmp/kjb_params.txt
```

### 4.5 文件引用完整性检查

```bash
# 检查 KJB 中引用的 KTR 文件是否存在于仓库中
# KJB 中引用了以下 KTR：
#   - ${ETL_DIRECTORY}${P_ETL_Transformation_Directory}${P_ETL_Transformation_Name}.ktr（动态，运行时确定）
#   - ${ETL_DIRECTORY}/Utilities/ETL_Completion_E-Mail.ktr
#   - ${ETL_DIRECTORY}Utilities/ETL_Specific_To_Addresses.ktr

# 验证静态引用的 Utilities KTR 是否存在
ls ETL/Utilities/ETL_Completion_E-Mail.ktr
ls ETL/Utilities/ETL_Specific_To_Addresses.ktr
```

### 4.6 _control.json 格式验证

```bash
# 注意：_control.json 包含注释（// Edit this line...），不是标准 JSON
# 需先移除注释再验证
sed 's|//.*||' ETL/_control.json | python -m json.tool
```

### 4.7 可静态验证 vs 需外部环境的部分汇总

| 验证内容 | 是否可静态验证 | 说明 |
|----------|:-------------:|------|
| Shell 脚本语法 | **可以** | `bash -n` / ShellCheck |
| KJB/KTR XML 格式 | **可以** | xmllint |
| 参数传递一致性 | **可以** | grep + diff 比对参数名 |
| 静态文件引用 | **可以** | 检查 Utilities/ 下的 KTR 是否存在 |
| _control.json 格式 | **可以** | 移除注释后用 JSON 解析器验证 |
| 路径常量正确性 | **可以** | 检查 ETL_ROOT_DIR 等硬编码路径的格式 |
| kitchen.sh 实际执行 | **不可以** | 需安装 Pentaho Data Integration |
| KTR 转换逻辑 | **不可以** | 需 Kettle/Spoon GUI 或 Pan 命令行 |
| 数据库连接 | **不可以** | 需配置数据库及连接信息 |
| 邮件发送 | **不可以** | 需 SMTP 服务器配置 |
| DataSync 数据发布 | **不可以** | 需目标平台（如 Socrata）的 API 凭据和端点 |
| 日志写入 | **部分可以** | 可检查 `../Log/` 目录是否存在，但完整日志需实际运行 |

---

## 五、注意事项与潜在问题

1. **硬编码路径**：`ETL_Setup.sh` 中的 `ETL_ROOT_DIR` 和 JDK 路径均为占位符（`/path/to/...`），部署时必须替换为实际路径。

2. **相对路径依赖**：`Standard_ETL.sh` 中使用 `../data-integration/kitchen.sh`，这要求脚本必须从 `ETL/` 目录执行，否则路径解析会出错。

3. **Source 执行方式**：`Setup-Script.sh` 使用 `. $ETL_ROOT_DIR/Standard_ETL.sh` (source) 而非 `bash` 子进程调用，意味着 `Standard_ETL.sh` 中的变量变更会影响调用方的 shell 环境。

4. **日志目录**：日志输出到 `../Log/`，该目录不在仓库版本控制中，需在部署时手动创建。

5. **_control.json 非标准 JSON**：文件中包含 JavaScript 风格注释（`// Edit this line...`），标准 JSON 解析器会报错，需注意处理。

6. **KJB 路径不一致**：`ETL_Completion_E-Mail.ktr` 的引用路径为 `${ETL_DIRECTORY}/Utilities/...`（带前导 `/`），而 `ETL_Specific_To_Addresses.ktr` 的路径为 `${ETL_DIRECTORY}Utilities/...`（无前导 `/`），这取决于 `ETL_DIRECTORY` 变量是否以 `/` 结尾，存在潜在的路径拼接风险。
