# Kettle ETL 框架代码解析

## 一、核心文件总览

| 文件 | 类型 | 职责 |
|------|------|------|
| `ETL/_control.json` | DataSync 控制配置 | 定义数据上传到 Socrata 的 CSV 解析规则与列映射 |
| `ETL/ETL_Template.ktr` | Kettle 转换模板 (.ktr) | 数据抽取 → 落文件 → 调用 DataSync 上传的标准数据流 |
| `ETL/Standard_ETL.kjb` | Kettle 作业 (.kjb) | 编排入口：设置变量 → 运行 KTR → 根据结果发送邮件通知 |
| `ETL/Standard_ETL.sh` | Shell 启动脚本 | 调用 `kitchen.sh` 执行 `.kjb`，并将日志输出到 `../Log/` |
| `ETL/ETL_Setup.sh` | Shell 环境初始化 | 设置 `NOW`（时间戳）、`PATH`、`ETL_ROOT_DIR` |
| `ETL/Setup-Script.sh` | 单数据集调度模板 | 为每个数据集编写的入口脚本，设置名称/目录/DataSync 开关后调用 `Standard_ETL.sh` |

---

## 二、三层调用关系

### 2.1 整体链路

```
Setup-Script.sh（每个数据集一份）
  └─ source ETL_Setup.sh            # 初始化环境变量
  └─ source Standard_ETL.sh         # 传入 ETL_NAME / ETL_DIR_RELATIVE / DATA_SYNC_ETL
       └─ kitchen.sh -file=Standard_ETL.kjb   # Kettle Job 引擎
            ├─ START
            ├─ Set variables                   # 将参数提升为 Job 级变量
            ├─ ETL_Specific_To_Addresses.ktr   # 获取邮件收件人列表
            ├─ Run KTR ──────────────────────► ${ETL_DIRECTORY}${P_ETL_Transformation_Directory}${P_ETL_Transformation_Name}.ktr
            │    （即基于 ETL_Template.ktr 定制的转换）
            │    ├─ Table input                # SQL 抽取源数据
            │    ├─ Get Variables              # 读取 P_File / P_DatasetID / P_ControlFile
            │    ├─ Text file output           # 写 CSV 到 files/ 子目录
            │    ├─ Prepare_for_DataSyncSSync  # 调用 Utilities/Prepare_for_DataSyncSSync.ktr
            │    ├─ Wait for text file         # 阻塞直到 CSV 写完
            │    ├─ DataSync HTTP              # 调用 Utilities/DataSyncHTTP.ktr 上传
            │    ├─ DSResult                   # 组装上传结果字符串
            │    └─ Copy rows to result        # 回传给 Job
            │
            ├─ [成功] ETL_Completion_E-Mail (TRUE)    # 发送成功邮件
            └─ [失败] ETL_Completion_E-Mail (FALSE)   # 发送失败邮件
```

### 2.2 关键变量传递

| 变量名 | 来源 | 消费方 | 说明 |
|--------|------|--------|------|
| `ETL_DIRECTORY` | `kettle.properties` 全局配置 | `.kjb` / `.ktr` | ETL 框架根目录，所有子转换路径的基准 |
| `DATA_SYNC_DIRECTORY` | `kettle.properties` 全局配置 | DataSync 子转换 | DataSync JAR 所在目录 |
| `P_ETL_Transformation_Directory` | `.kjb` 参数 | `Run KTR` 步骤 | 数据集 KTR 相对于 `ETL_DIRECTORY` 的子目录路径（以 `/` 结尾） |
| `P_ETL_Transformation_Name` | `.kjb` 参数 | `Run KTR` 步骤 | KTR 文件名（不含 `.ktr` 后缀） |
| `P_Data_Sync_ETL` | `.kjb` 参数（默认 `N`） | 邮件通知 | 是否为 DataSync ETL，仅影响邮件正文 |
| `P_ControlFile` | `.ktr` 参数 | Get Variables → DataSync | 控制文件名（如 `Data_Set_Name_control.json`） |
| `P_DatasetID` | `.ktr` 参数 | Get Variables → DataSync | Socrata 数据集 4x4 ID |
| `P_File` | `.ktr` 参数 | Get Variables → Text file output | 落地 CSV 文件名 |

### 2.3 `_control.json` 与 KTR 的协作

`_control.json` 是 DataSync 上传阶段的控制文件，在 KTR 中通过以下路径被读取：

1. `Get Variables` 步骤将 `P_ControlFile` 拼接为绝对路径：`${Internal.Transformation.Filename.Directory}/${P_ControlFile}`
2. 该路径传递给 `Prepare_for_DataSyncSSync` 子转换
3. 再传给 `DataSync HTTP` 子转换，最终由 DataSync 引擎读取

控制文件中的 `columns` 数组必须与 KTR 中 `Text file output` 步骤输出的列名**一一对应**，否则 DataSync 上传将失败。

---

## 三、新建数据集 ETL 所需文件与配置

### 3.1 需要创建的文件

假设新数据集名为 `My_Dataset`，4x4 ID 为 `abcd-1234`：

```
ETL/
├── My_Dataset_abcd-1234/                          # 新建目录
│   ├── My_Dataset_abcd-1234.ktr                   # 从 ETL_Template.ktr 复制并定制
│   ├── My_Dataset_control.json                    # 从 _control.json 复制并定制
│   └── files/                                     # 运行时自动创建，存放输出 CSV
└── ...
```

### 3.2 需要修改的内容

#### (a) KTR 转换文件 (`My_Dataset_abcd-1234.ktr`)

| 修改位置 | 操作 |
|----------|------|
| **参数** `P_ControlFile` | 默认值填写 `My_Dataset_control.json` |
| **参数** `P_DatasetID` | 默认值填写 `abcd-1234` |
| **参数** `P_File` | 默认值填写 `My_Dataset_abcd-1234.csv` |
| **Table input** 步骤 | 替换 `SELECT <values> FROM <table name> WHERE <conditions>` 为实际 SQL |
| **Table input** 的数据库连接 | 配置实际的数据库连接 |
| 自定义转换步骤 | 可在 `Table input` 与 `Get Variables` 之间添加字段映射、类型转换等 |

**注意：** 以下步骤为模板固定步骤，**不应修改**：
- Get Variables
- Prepare_for_DataSyncSSync (sub-transformation)
- Wait for text file
- DataSync HTTP (sub-transformation)
- DSResult
- Copy rows to result
- Text file output

#### (b) 控制文件 (`My_Dataset_control.json`)

```json
{
  "action" : "Replace",
  "csv" : {
    "columns" : ["actual_col_1", "actual_col_2", "actual_col_3"],
    ...
  }
}
```

- 将 `columns` 数组替换为 Socrata 数据集中实际的 API 字段名
- 根据需要调整 `action`（`Replace` 全量替换 / 其他模式）
- 根据源数据调整 `separator`、`encoding`、`fixedTimestampFormat` 等

#### (c) 调度脚本 (可选，用于自动化)

复制 `Setup-Script.sh` 并修改三个变量：

```bash
ETL_NAME=My_Dataset_abcd-1234
ETL_DIR_RELATIVE=My_Dataset_abcd-1234/
DATA_SYNC_ETL=Y
```

---

## 四、静态验证清单

以下清单可在代码审查或部署前对 ETL 配置进行静态检查，无需实际运行作业。

### 4.1 参数命名

| # | 检查项 | 预期 | 验证方法 |
|---|--------|------|----------|
| 1 | KTR 中是否声明了 `P_ControlFile`、`P_DatasetID`、`P_File` 三个参数 | 三个参数均存在于 `<parameters>` 节点 | 在 `.ktr` XML 中搜索 `<name>P_ControlFile</name>`、`<name>P_DatasetID</name>`、`<name>P_File</name>` |
| 2 | `P_DatasetID` 的默认值是否符合 4x4 格式 | 匹配正则 `^[a-z0-9]{4}-[a-z0-9]{4}$` | 提取 `<default_value>` 并校验 |
| 3 | `P_ControlFile` 的默认值是否以 `_control.json` 结尾 | 以 `_control.json` 结尾 | 字符串匹配 |
| 4 | `P_File` 的默认值是否以 `.csv` 结尾 | 以 `.csv` 结尾 | 字符串匹配 |
| 5 | KJB 中 `P_ETL_Transformation_Name` 是否与实际 KTR 文件名（去后缀）一致 | 完全匹配 | 比较参数值与目录中 `.ktr` 文件名 |
| 6 | KJB 中 `P_ETL_Transformation_Directory` 是否以 `/` 结尾 | 以 `/` 结尾 | 字符串匹配 |

### 4.2 目录约定

| # | 检查项 | 预期 | 验证方法 |
|---|--------|------|----------|
| 7 | 数据集目录命名是否符合 `Name_abcd-1234` 格式 | 目录名包含 4x4 ID | 正则匹配目录名 `.*-[a-z0-9]{4}-[a-z0-9]{4}$` |
| 8 | KTR 文件名与所在目录名是否一致 | `目录名.ktr` 存在于该目录下 | `ls ETL/<dir>/<dir>.ktr` |
| 9 | 控制文件是否存在于数据集目录中 | `P_ControlFile` 指定的文件存在 | `ls ETL/<dir>/<control_file>` |
| 10 | `ETL_DIRECTORY` 环境变量是否在 `kettle.properties` 中配置 | 变量已定义且路径存在 | 读取 `$HOME/.kettle/kettle.properties`，检查 `ETL_DIRECTORY` 行 |
| 11 | `DATA_SYNC_DIRECTORY` 环境变量是否已配置 | 变量已定义且路径存在 | 同上，检查 `DATA_SYNC_DIRECTORY` 行 |
| 12 | 仓库根目录下 `Log/` 目录是否存在 | 存在且可写 | `test -d ../Log && test -w ../Log` |

### 4.3 DataSync 开关

| # | 检查项 | 预期 | 验证方法 |
|---|--------|------|----------|
| 13 | `P_Data_Sync_ETL` 参数值是否为 `Y` 或 `N` | 仅为 `Y` 或 `N` | 提取 KJB 中 `<default_value>` 或 Shell 脚本中 `DATA_SYNC_ETL` 变量值 |
| 14 | 当 `DATA_SYNC_ETL=Y` 时，控制文件 `columns` 是否已填写实际列名 | 不含占位符 `api_name_*` | 解析 JSON 检查 `columns` 数组元素不匹配 `api_name_\d+` |
| 15 | 控制文件 JSON 语法是否合法 | 无语法错误 | 注意：模板中 `columns` 行有 `//` 注释，这在标准 JSON 中非法；实际使用时必须删除注释 |
| 16 | `datasync.jar` 是否存在于 `DATA_SYNC_DIRECTORY` | 文件存在 | `test -f $DATA_SYNC_DIRECTORY/datasync.jar` |

### 4.4 邮件通知

| # | 检查项 | 预期 | 验证方法 |
|---|--------|------|----------|
| 17 | `ETL_Completion_E-Mail.ktr` 中是否配置了 SMTP 参数 | `P_SMTP_Server`、`P_SMTP_Port`、`P_Sender_Address`、`P_Sender_Name`、`P_To_Address` 均有有效值 | 打开 `Utilities/ETL_Completion_E-Mail.ktr`，检查 `<parameters>` 段 |
| 18 | KJB 中 TRUE/FALSE 两条路径是否都接入了邮件步骤 | `<hop>` 中 `Run KTR → E-Mail TRUE`（evaluation=Y）和 `Run KTR → E-Mail FALSE`（evaluation=N）均存在且 `enabled=Y` | 解析 KJB XML 的 `<hops>` 节点 |
| 19 | `ETL_Specific_To_Addresses.ktr` 是否在 Run KTR 之前执行 | hop 顺序：`Set variables → ETL_Specific_To_Addresses → Run KTR` | 解析 KJB 的 `<hops>` 验证执行顺序 |
| 20 | `P_Body_Start` 是否在 TRUE 和 FALSE 两个邮件步骤中分别设置了不同内容 | TRUE 路径包含"ran to completion"，FALSE 路径包含"did not run to completion" | 搜索 KJB 中两个 entry 的 `<value>` 字段 |

### 4.5 日志输出位置

| # | 检查项 | 预期 | 验证方法 |
|---|--------|------|----------|
| 21 | `Standard_ETL.sh` 日志输出是否指向 `../Log/` | 重定向目标为 `../Log/${ETL_NAME}_${NOW}.txt` | 读取 shell 脚本中 `>` 重定向目标 |
| 22 | 日志文件名是否包含 ETL 名称和时间戳 | 格式为 `<ETL_NAME>_<YYYYMMDD_HHMMSS>.txt` | 正则 `^.+_\d{8}_\d{6}\.txt$` |
| 23 | `NOW` 变量是否在 `ETL_Setup.sh` 中正确初始化 | `NOW=$(date +"%Y%m%d_%H%M%S")` | 读取脚本内容 |
| 24 | Kitchen 日志级别是否设置为 `Detailed` 或更高 | `-level=Detailed`（或 `Debug`、`Rowlevel`） | 读取 `Standard_ETL.sh` 中 kitchen.sh 调用参数 |
| 25 | KTR/KJB 中的日志表配置是否一致（如果启用数据库日志） | `trans-log-table` 和 `job-log-table` 的 `<connection>` 和 `<table>` 已填写或均为空 | 解析 XML 中日志表节点 |

---

## 五、外部环境依赖

| 依赖项 | 说明 | 验证方式 |
|--------|------|----------|
| **Java JDK** | Kettle 运行时必需；路径需在 `ETL_Setup.sh` 的 `PATH` 中 | `java -version` |
| **Kettle (PDI)** | 数据集成引擎；需通过 `data-integration` 软链接指向实际版本目录 | `ls -la data-integration` 检查软链接 |
| **Socrata DataSync** | 数据上传工具（`datasync.jar`）；仅适用于 Socrata 平台 | `java -jar datasync.jar --version` |
| **数据库连接** | KTR 中 `Table input` 步骤需要有效的 JDBC 连接和驱动 | Kettle 中测试数据库连接 |
| **SMTP 服务** | 邮件通知需要可访问的 SMTP 服务器 | `telnet <SMTP_Server> <SMTP_Port>` |
| **Socrata 平台账号** | DataSync headless 模式需要 domain、username、password、app token | 检查 `DataSync/config.json` |
| **kettle.properties** | 必须配置 `ETL_DIRECTORY` 和 `DATA_SYNC_DIRECTORY` 两个全局变量 | 读取 `$HOME/.kettle/kettle.properties` |
| **cron / 任务调度器** | 生产环境下通过 cron 定时调用 `Setup-Script.sh` | `crontab -l` |

---

## 六、关键注意事项

1. **模板中的 JSON 注释**：`_control.json` 模板第 16 行有 `// Edit this line...` 注释，标准 JSON 不支持注释，实际使用时**必须删除**，否则 DataSync 解析失败。

2. **columns 与 CSV 输出对齐**：控制文件中 `columns` 数组的列名必须与 KTR 中 `Text file output` 步骤输出的字段名和顺序完全一致。

3. **路径分隔符**：所有路径配置建议使用正斜杠 `/`（即使在 Windows 上），以保持跨平台兼容性。

4. **ETL_Setup.sh 中的硬编码路径**：`ETL_ROOT_DIR` 和 JDK `PATH` 在模板中是占位符（`/path/to/...`），部署时必须替换为实际路径。

5. **从模板slave server 配置**：`ETL_Template.ktr` 中包含一个硬编码的 slave server 配置（IP: `10.96.12.40`，用户: `jlevy`），新建 ETL 时应根据实际环境清理或更新。
