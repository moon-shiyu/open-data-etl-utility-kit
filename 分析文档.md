# ETL 标准启动链路代码解析

## 一、完整执行链路

### 1.1 总体调用关系

```
Setup-Script.sh
  ├── source ETL_Setup.sh          (加载环境变量)
  ├── 设置 ETL_NAME / ETL_DIR_RELATIVE / DATA_SYNC_ETL
  └── source Standard_ETL.sh       (传参调用)
        └── kitchen.sh             (Pentaho/Kettle CLI)
              └── Standard_ETL.kjb (Kettle Job)
                    ├── Set variables
                    ├── ETL_Specific_To_Addresses.ktr
                    ├── Run KTR  →  用户自定义 .ktr 文件
                    ├── (成功) ETL_Completion_E-Mail TRUE
                    └── (失败) ETL_Completion_E-Mail FALSE
```

### 1.2 各阶段详解

#### 阶段 1：加载环境变量 — `ETL_Setup.sh`

```bash
NOW=$(date +"%Y%m%d_%H%M%S")                             # 时间戳，用于日志文件名
PATH=$PATH:/path/to/jdk/bin                               # 将 JDK 加入 PATH
ETL_ROOT_DIR=/path/to/directory/open-data-etl-utility-kit/ETL  # ETL 工具包根目录
```

| 变量 | 作用 |
|------|------|
| `NOW` | 生成形如 `20260616_143025` 的时间戳，拼入日志文件名 |
| `PATH` | 确保后续 `kitchen.sh` 能找到 Java 运行时 |
| `ETL_ROOT_DIR` | 所有后续路径的基准目录 |

该文件通过 `. (source)` 方式被加载，因此其中定义的变量在后续所有脚本中均可见。

#### 阶段 2：设置 ETL 参数 — `Setup-Script.sh`

```bash
. /path/to/directory/open-data-etl-utility-kit/ETL/ETL_Setup.sh   # 第 1 行：加载环境

ETL_NAME=Name_of_KTR_file_no_extension      # KTR 转换文件名（不含 .ktr 后缀）
ETL_DIR_RELATIVE=Directory_containing_KTR_file/   # KTR 文件所在的相对目录（以 / 结尾）
DATA_SYNC_ETL=Y                              # 是否为 DataSync 类型 ETL（Y/N）

echo "ETL $ETL_NAME has started at $NOW."    # 控制台输出启动信息

. $ETL_ROOT_DIR/Standard_ETL.sh $ETL_NAME $ETL_DIR_RELATIVE $DATA_SYNC_ETL
```

这是一个**模板文件**，每个具体的 ETL 任务需要复制此文件并填写真实的 `ETL_NAME`、`ETL_DIR_RELATIVE`、`DATA_SYNC_ETL` 值。

三个核心参数含义：

| 参数 | 示例值 | 说明 |
|------|--------|------|
| `ETL_NAME` | `My_Sales_ETL` | 对应 `ETL/` 下某个 `.ktr` 文件的文件名（无扩展名） |
| `ETL_DIR_RELATIVE` | `Sales/` | `.ktr` 文件相对于 `ETL_ROOT_DIR` 的子目录路径，须以 `/` 结尾 |
| `DATA_SYNC_ETL` | `Y` 或 `N` | 标记是否需要通过 DataSync 将结果推送到数据门户 |

#### 阶段 3：调用 kitchen.sh — `Standard_ETL.sh`

```bash
# 接收位置参数
ETL_NAME=$1
ETL_DIR_RELATIVE=$2
DATA_SYNC_ETL=$3

# 调用 Pentaho Kitchen CLI 执行 Kettle Job
../data-integration/kitchen.sh \
  -file="$ETL_ROOT_DIR/Standard_ETL.kjb" \
  -param:P_ETL_Transformation_Directory=$ETL_DIR_RELATIVE \
  -param:P_ETL_Transformation_Name=$ETL_NAME \
  -param:P_Data_Sync_ETL=$DATA_SYNC_ETL \
  -level=Detailed \
  > "../Log/${ETL_NAME}_${NOW}.txt"
```

关键点：
- **`kitchen.sh`** 是 Pentaho Data Integration（PDI/Kettle）的命令行 Job 执行器，位于 `../data-integration/` 目录
- **`-file`** 指定要执行的 Job 文件为 `Standard_ETL.kjb`
- **`-param:`** 前缀将 Shell 变量映射为 Kettle Job 参数
- **`-level=Detailed`** 设置日志级别为详细模式
- **`>`** 将所有标准输出重定向到 `../Log/${ETL_NAME}_${NOW}.txt`（日志文件）

#### 阶段 4：Kettle Job 执行 — `Standard_ETL.kjb`

`.kjb` 是 Kettle Job 的 XML 定义文件，其内部执行流程为：

```
START (入口)
  │ (unconditional)
  ▼
Set variables
  │  设置 Data_Sync_ETL、ETL_Transformation_Name、ETL_Specific_To_Addresses
  │ (unconditional)
  ▼
ETL_Specific_To_Addresses.ktr
  │  读取并设置 ETL 特定的邮件收件人列表
  │ (unconditional)
  ▼
Run KTR
  │  执行: ${ETL_DIRECTORY}${P_ETL_Transformation_Directory}${P_ETL_Transformation_Name}.ktr
  │  即用户自定义的数据转换逻辑
  ├── (evaluation=Y, 成功) ──▶ ETL_Completion_E-Mail - TRUE
  │     发送成功通知邮件
  └── (evaluation=N, 失败) ──▶ ETL_Completion_E-Mail - FALSE
        发送失败通知邮件
```

Job 定义的三个参数及其默认值：

| 参数名 | 默认值 | 描述 |
|--------|--------|------|
| `P_Data_Sync_ETL` | `N` | 是否为 DataSync ETL |
| `P_ETL_Transformation_Directory` | _(空)_ | KTR 文件的相对目录路径 |
| `P_ETL_Transformation_Name` | _(空)_ | KTR 转换名称（无扩展名） |

#### 阶段 5：数据转换模板 — `ETL_Template.ktr`

`.ktr` 是 Kettle Transformation 的定义文件。模板中展示了一个典型的 DataSync ETL 数据流：

```
Table input (SQL 查询)
  │
  ▼
Get Variables (获取 File、DatasetID、ControlFile、File_to_Geocode)
  ├──▶ Text file output (输出 CSV 文件)
  │
  ▼
Prepare_for_DataSyncSSync (子转换)
  │
  ▼
Wait for text file (等待文件写入完成)
  │
  ▼
DataSync HTTP (子转换，调用 DataSyncHTTP.ktr)
  │
  ▼
DSResult (JavaScript 脚本，组装结果信息)
  │
  ▼
Copy rows to result (将结果行传回 Job)
```

转换参数：

| 参数 | 说明 |
|------|------|
| `P_ControlFile` | DataSync JSON 控制文件名（如 `_control.json`） |
| `P_DatasetID` | 目标数据集的 4x4 ID |
| `P_File` | DataSync 使用的数据文件名 |

## 二、日志与配置路径说明

### 2.1 目录结构假设

```
open-data-etl-utility-kit/
├── data-integration/          # Pentaho Data Integration (Kettle) 安装目录
│   └── kitchen.sh             # Job 命令行执行器
├── Log/                       # 日志输出目录
│   └── {ETL_NAME}_{NOW}.txt   # 格式: My_Sales_ETL_20260616_143025.txt
└── ETL/                       # ETL 脚本与定义文件
    ├── ETL_Setup.sh
    ├── Setup-Script.sh
    ├── Standard_ETL.sh
    ├── Standard_ETL.kjb
    ├── ETL_Template.ktr
    ├── _control.json           # DataSync CSV 控制文件
    └── Utilities/
        ├── DataSync.ktr
        ├── DataSyncFTP.ktr
        ├── DataSyncHTTP.ktr
        ├── ETL_Completion_E-Mail.ktr
        ├── ETL_Specific_To_Addresses.ktr
        ├── Prepare_for_DataSyncFTP.ktr
        ├── Prepare_for_DataSyncHTTP.ktr
        └── Prepare_for_DataSyncSSync.ktr
```

### 2.2 日志路径解析

日志路径在 `Standard_ETL.sh` 第 7 行定义：

```
"../Log/${ETL_NAME}_${NOW}.txt"
```

- `../Log/` — 相对于 `ETL/` 目录的上级 `Log/` 目录
- `${ETL_NAME}` — 来自 `Setup-Script.sh` 设定的转换名称
- `${NOW}` — 来自 `ETL_Setup.sh` 生成的时间戳 `YYYYmmdd_HHMMSS`
- 使用 `${}` 花括号语法是为了避免 `${ETL_NAME}_${NOW}` 中下划线被误解析为变量名的一部分

## 三、静态验证方法（无 Kettle/DataSync 环境）

在本地没有安装 Pentaho Data Integration（Kettle）和 DataSync 的情况下，可以对以下部分进行静态验证：

### 3.1 可静态验证的部分

#### 1) Shell 脚本语法检查

```bash
# 使用 bash -n 进行语法检查（不实际执行）
bash -n ETL/ETL_Setup.sh
bash -n ETL/Setup-Script.sh
bash -n ETL/Standard_ETL.sh
```

可验证内容：
- 语法是否正确（括号匹配、引号闭合等）
- source 语句路径格式是否合理
- 变量引用是否正确使用了 `${}` 语法

#### 2) ShellCheck 静态分析

```bash
# 安装 ShellCheck 后
shellcheck ETL/ETL_Setup.sh
shellcheck ETL/Setup-Script.sh
shellcheck ETL/Standard_ETL.sh
```

可发现的问题：
- 变量未加引号导致的潜在路径空格问题
- 未声明的变量引用
- 不安全的参数扩展

#### 3) Kettle XML 文件结构验证

```bash
# 验证 .kjb / .ktr 文件是否为合法 XML
xmllint --noout ETL/Standard_ETL.kjb
xmllint --noout ETL/ETL_Template.ktr
```

可验证内容：
- XML 格式是否良构（well-formed）
- 标签是否正确闭合
- 实体编码（如 `&#47;` 对应 `/`）是否正确

#### 4) JSON 控制文件验证

```bash
# 注意：_control.json 中包含 JavaScript 风格注释，需先去除
# 使用 python 验证（去除注释后）
python -c "
import json, re
with open('ETL/_control.json') as f:
    content = re.sub(r'//.*', '', f.read())
    json.loads(content)
    print('JSON valid')
"
```

**注意**：当前 `_control.json` 第 16 行包含 `// Edit this line...` 注释，这不是标准 JSON 语法，仅部分解析器（如 Socrata DataSync）支持。

#### 5) 参数传递链一致性检查

可以手动或通过脚本验证参数名在各层之间是否一致：

| Shell 层变量 | kitchen.sh `-param:` 参数 | .kjb 中声明的参数 |
|-------------|--------------------------|------------------|
| `$ETL_DIR_RELATIVE` (=$2) | `P_ETL_Transformation_Directory` | `P_ETL_Transformation_Directory` |
| `$ETL_NAME` (=$1) | `P_ETL_Transformation_Name` | `P_ETL_Transformation_Name` |
| `$DATA_SYNC_ETL` (=$3) | `P_Data_Sync_ETL` | `P_Data_Sync_ETL` |

验证结论：**三层参数名完全一致，传递链路正确。**

#### 6) 文件引用完整性检查

验证 `.kjb` 和 `.ktr` 中引用的文件是否存在于仓库中：

| 引用位置 | 引用的文件路径 | 仓库中是否存在 |
|----------|---------------|-------------- |
| `Standard_ETL.kjb` → Run KTR | `${ETL_DIRECTORY}${P_ETL_Transformation_Directory}${P_ETL_Transformation_Name}.ktr` | 取决于具体 ETL 配置 |
| `Standard_ETL.kjb` → E-Mail | `${ETL_DIRECTORY}/Utilities/ETL_Completion_E-Mail.ktr` | 存在 |
| `Standard_ETL.kjb` → To_Addresses | `${ETL_DIRECTORY}Utilities/ETL_Specific_To_Addresses.ktr` | 存在 |
| `ETL_Template.ktr` → DataSyncHTTP | `${ETL_DIRECTORY}Utilities/DataSyncHTTP.ktr` | 存在 |
| `ETL_Template.ktr` → Prepare | `${ETL_DIRECTORY}Utilities/Prepare_for_DataSyncSSync.ktr` | 存在 |

### 3.2 需要外部环境才能运行的部分

以下部分**无法**在本地仅通过静态分析完成验证，必须依赖外部运行时环境：

| 组件 | 依赖 | 说明 |
|------|------|------|
| `kitchen.sh` | Pentaho Data Integration (PDI/Kettle) | Job 执行引擎，解析并运行 `.kjb` 文件 |
| `Table input` 步骤 | 数据库连接 | `ETL_Template.ktr` 中的 SQL 查询需要实际数据库 |
| `DataSyncHTTP.ktr` | Socrata DataSync + 网络访问 | 通过 HTTP 将数据推送到 Socrata 开放数据门户 |
| `Prepare_for_DataSyncSSync.ktr` | DataSync CLI 工具 | 准备 DataSync 执行环境 |
| `ETL_Completion_E-Mail.ktr` | SMTP 邮件服务器 | 发送 ETL 完成通知邮件 |
| `ETL_Specific_To_Addresses.ktr` | 邮件地址配置 | 获取 ETL 特定的收件人列表 |
| JDK 运行时 | Java | Kettle 本身基于 Java，`kitchen.sh` 内部调用 Java |
| 日志目录 `../Log/` | 文件系统 | 需要该目录预先存在且有写权限 |

### 3.3 推荐的本地验证流程

```
1. bash -n *.sh               # Shell 语法检查
2. shellcheck *.sh             # Shell 静态分析
3. xmllint --noout *.kjb *.ktr # XML 格式验证
4. 人工检查参数传递链一致性       # 对照上表
5. 人工检查文件引用完整性         # 对照上表
6. 检查 _control.json 格式      # 注意非标准注释
```

以上步骤可在无任何外部依赖的纯开发环境中完成，能有效发现配置错误、路径拼写错误和格式问题，但无法验证数据流的实际运行结果。
