# Socrata DataSync 上传配置 — 代码解析文档

## 1. 整体架构概述

本项目使用 **Pentaho Kettle (PDI)** 作为 ETL 引擎，将清洗/转换后的数据文件通过 **Socrata DataSync** 命令行工具发布到 Socrata 开放数据平台。整个链路分为两个阶段：

```
┌─────────────────────────────────────────────────────────────────────┐
│  阶段一（一次性）: 凭据初始化                                         │
│  config_template.json → config.json → load_preferences.sh          │
│  → DataSync 内部存储（"memory"）                                    │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│  阶段二（周期性）: ETL 数据发布                                       │
│  Kettle 父作业 → DataSync.ktr / DataSyncFTP.ktr / DataSyncHTTP.ktr │
│  → java -jar datasync.jar（免 config.json，依赖已加载的凭据）         │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 2. 各文件职责详解

### 2.1 `DataSync/config_template.json` — 凭据与偏好模板

**位置**: `DataSync/config_template.json`

该文件是 Socrata DataSync 的配置模板，用于一次性写入 DataSync 内部存储。包含以下配置域：

| 配置项 | 示例值 | 说明 |
|--------|--------|------|
| `domain` | `https://data.metropolis.gov` | Socrata 实例的 API 域名 |
| `username` | `superman@metropolis.gov[...]` | Socrata 用户名；模板中带有防误操作的方括号后缀 |
| `password` | `[Redacted]` | Socrata 用户密码 |
| `appToken` | `[Redacted]` | Socrata API App Token |
| `adminEmail` | `clark.kent@metropolis.gov` | 出错时接收告警的管理员邮箱 |
| `emailUponError` | `true` | 是否在上传失败时发送邮件告警 |
| `logDatasetID` | `abcd-1234` | Socrata 上用于记录 DataSync 运行日志的数据集 ID |
| `outgoingMailServer` | `smtp.metropolis.gov` | SMTP 服务器地址 |
| `smtpPort` | `25` | SMTP 端口 |
| `sslPort` | (空) | SSL 端口（未启用） |
| `smtpUsername` / `smtpPassword` | (空) | SMTP 认证凭据（当前未配置） |
| `filesizeChunkingCutoffMB` | `10` | 超过此大小的文件将分块上传（MB） |
| `numRowsPerChunk` | `50000` | 每块包含的最大行数 |

**关键设计**: `username` 字段故意保留了 `[This part in brackets...]` 后缀——这是一个安全防护措施，防止在未填写真实密码时意外以有效用户名触发认证锁定。

### 2.2 `DataSync/load_preferences.sh` — 凭据加载脚本

**位置**: `DataSync/load_preferences.sh`

```bash
java -jar datasync.jar --config config.json --jobType LoadPreferences
```

**职责**:
- 将 `config.json`（从模板复制并填入真实凭据后的文件）一次性加载到 DataSync 的内部持久化存储中
- 加载完成后，后续的 DataSync 命令行调用无需再指定凭据参数
- 脚本注释明确要求：**使用后立即删除 `config.json`**，避免凭据文件残留

**操作流程**:
1. 复制 `config_template.json` → `config.json`
2. 在 `config.json` 中填入真实的 username、password、appToken
3. 移除 username 字段中的方括号防护后缀
4. 运行 `load_preferences.sh`
5. 删除 `config.json`

### 2.3 `ETL/Utilities/DataSync.ktr` — 标准 HTTP API 上传子转换

**位置**: `ETL/Utilities/DataSync.ktr`

这是一个 Kettle **子转换 (sub-transformation)**，通过 Mapping Input/Output 与父作业对接。

**数据流**:
```
Set Variable (Data_Sync_ETL=Y)
    ↓
Mapping Input（接收: file, header, datasetid, method）
    ↓
GetVariable（读取 ${DATA_SYNC_DIRECTORY}）
    ↓
Formula（拼接命令行）
    ↓
ExecProcess（执行 DataSync 命令）
    ↓ ─────→ WriteToLog（记录 stdout/stderr/exitCode）
Mapping Output（返回结果给父作业）
```

**生成的命令**:
```
java -jar {DATA_SYNC_DIRECTORY}datasync.jar \
  --fileToPublish {file} \
  --fileToPublishHasHeaderRow {header} \
  --datasetID {datasetid} \
  --publishMethod {method}
```

**输入字段**:
| 字段 | 类型 | 说明 |
|------|------|------|
| `file` | String | 待上传的 CSV/数据文件路径 |
| `header` | String | 文件是否包含表头行 (true/false) |
| `datasetid` | String | 目标 Socrata 数据集的四段式 ID (xxxx-xxxx) |
| `method` | String | 发布方式 (replace/upsert/append 等) |

**特点**: 发布方式 (`method`) 由调用方动态传入，灵活性最高。

### 2.4 `ETL/Utilities/DataSyncFTP.ktr` — FTP 通道上传子转换

**位置**: `ETL/Utilities/DataSyncFTP.ktr`

**生成的命令**:
```
java -jar {DATA_SYNC_DIRECTORY}datasync.jar \
  --fileToPublish {file} \
  --datasetID {datasetid} \
  --publishMethod replace \
  --publishViaFTP true \
  --pathToFTPControlFile {controlfile}
```

**输入字段**:
| 字段 | 类型 | 说明 |
|------|------|------|
| `file` | String | 待上传的数据文件路径 |
| `datasetid` | String | 目标 Socrata 数据集 ID |
| `controlfile` | String | FTP 控制文件路径（定义列映射等） |

**特点**:
- 发布方式硬编码为 `replace`
- 使用 Socrata 的 FTP 批量上传通道 (`--publishViaFTP true`)
- 需要额外的 FTP 控制文件指定列映射关系

### 2.5 `ETL/Utilities/DataSyncHTTP.ktr` — HTTP SmartUpdate 上传子转换

**位置**: `ETL/Utilities/DataSyncHTTP.ktr`

**生成的命令**:
```
java -jar -Xmx1g {DATA_SYNC_DIRECTORY}datasync.jar \
  --fileToPublish {file} \
  --datasetID {datasetid} \
  --publishMethod replace \
  --publishViaHttp true \
  --pathToControlFile {controlfile}
```

**输入字段**: 与 DataSyncFTP.ktr 相同 (`file`, `datasetid`, `controlfile`)。

**特点**:
- 发布方式硬编码为 `replace`
- 使用 HTTP SmartUpdate 通道 (`--publishViaHttp true`)
- JVM 堆内存设为 1GB (`-Xmx1g`)，适用于大数据量上传场景
- 控制文件参数名为 `--pathToControlFile`（区别于 FTP 的 `--pathToFTPControlFile`）

---

## 3. 三种 DataSync 变体对比

| 维度 | DataSync.ktr | DataSyncFTP.ktr | DataSyncHTTP.ktr |
|------|-------------|-----------------|------------------|
| 上传通道 | 标准 Socrata API | FTP 批量通道 | HTTP SmartUpdate |
| publishMethod | 调用方传入（动态） | 硬编码 `replace` | 硬编码 `replace` |
| 控制文件 | 不需要 | 需要（FTP 控制文件） | 需要（HTTP 控制文件） |
| 是否声明表头 | 是 (`header` 字段) | 否 | 否 |
| JVM 内存 | 默认 | 默认 | `-Xmx1g` |
| 适用场景 | 小/中型数据集，灵活发布 | 大型数据集 FTP 批量替换 | 大型数据集 HTTP 批量替换 |

---

## 4. 关键链路说明

### 4.1 凭据链路

```
config_template.json
        │  (手动复制 & 填写)
        ▼
   config.json
        │  (load_preferences.sh)
        ▼
DataSync 内部存储 ("Java Preferences API")
        │  (所有 .ktr 隐式依赖)
        ▼
  datasync.jar 命令行调用
```

所有三个 .ktr 子转换的注释中均明确声明：**"This subtransformation assumes the user ID, password, API key, and domain are already set in DataSync 'memory.'"** — 即它们不使用 config.json，而是依赖预先通过 `LoadPreferences` 加载的凭据。

### 4.2 Kettle 变量链路

```
kettle.properties
    │
    ├── DATA_SYNC_DIRECTORY → 所有 .ktr 中 GetVariable 步骤读取
    │                          用于定位 datasync.jar 的安装路径
    │
    └── 父作业/转换中的 Mapping 参数
            │
            ├── file      → ETL 输出文件路径
            ├── header    → 表头标识（仅 DataSync.ktr）
            ├── datasetid → Socrata 数据集 ID
            ├── method    → 发布方式（仅 DataSync.ktr）
            └── controlfile → 控制文件路径（仅 FTP/HTTP 变体）
```

### 4.3 标志变量

三个子转换都在流程起始处设置 `Data_Sync_ETL=Y`（作用域为 `ROOT_JOB`），供父作业判断当前 ETL 流程是否包含 DataSync 发布步骤。

---

## 5. 代码评审验证清单

以下为代码评审时需要检查的配置项、文件路径和外部依赖，**仅做静态验证使用，不涉及代码修改**。

### 5.1 配置项验证

| 序号 | 检查项 | 验证方法 | 风险等级 |
|------|--------|---------|---------|
| C-01 | `config_template.json` 中 `domain` 是否为正确的 Socrata 实例 URL | 目视确认域名 | 高 |
| C-02 | `username` 字段是否保留了方括号防护后缀 | 模板中应存在 `[This part...]` | 中 |
| C-03 | `password` 和 `appToken` 是否为占位符而非真实凭据 | 模板中应为 `[Redacted...]` | 高 |
| C-04 | `logDatasetID` (`abcd-1234`) 是否为有效的 Socrata 数据集 ID | 通过 Socrata API 验证该 ID 是否存在 | 中 |
| C-05 | `emailUponError` 是否为 `"true"` 或 `"false"` (字符串) | 确认 DataSync 预期的类型 | 低 |
| C-06 | `filesizeChunkingCutoffMB` 和 `numRowsPerChunk` 是否与数据量匹配 | 评估典型数据文件大小 | 低 |
| C-07 | SMTP 配置是否完整（当前 `smtpUsername`/`smtpPassword`/`sslPort` 为空） | 若 `emailUponError=true`，需确认 SMTP 25 端口是否支持匿名发送 | 中 |

### 5.2 文件路径验证

| 序号 | 检查项 | 验证方法 |
|------|--------|---------|
| F-01 | `kettle.properties` 中 `DATA_SYNC_DIRECTORY` 是否已配置且以 `/` 结尾 | 查看 `~/.kettle/kettle.properties` 或 Spoon 配置 |
| F-02 | `DATA_SYNC_DIRECTORY` 指向的目录下是否存在 `datasync.jar` | `ls ${DATA_SYNC_DIRECTORY}datasync.jar` |
| F-03 | 仓库中 `DataSync-1.5.3.jar` 是否已重命名为 `datasync.jar` | 文件名提示 "Rename to datasync.jar" |
| F-04 | `config.json`（真实凭据文件）是否已被删除、未提交到版本控制 | `git ls-files config.json`；检查 `.gitignore` |
| F-05 | 各 .ktr 中 Formula 拼接的路径是否正确（`DataSyncDirectory` 与 `datasync.jar` 之间无多余/缺失分隔符） | 当 `DATA_SYNC_DIRECTORY` 以 `/` 结尾时，拼接结果为 `{dir}datasync.jar`，路径正确 |
| F-06 | FTP/HTTP 变体所需的 `controlfile` 是否在 ETL 父作业中正确生成或指定 | 追溯调用方的 Mapping 输出 |

### 5.3 外部凭据与环境依赖

| 序号 | 依赖项 | 说明 | 验证方法 |
|------|--------|------|---------|
| E-01 | Socrata 用户账户 | 需要有目标数据集的写入权限 | 使用 API 测试 `GET /api/views/{datasetid}.json` |
| E-02 | Socrata App Token | 用于 API 频率限制豁免 | 确认 token 未过期 |
| E-03 | DataSync Java Preferences | `load_preferences.sh` 加载后的持久化凭据 | 在目标服务器上运行 `java -jar datasync.jar --jobType LoadPreferences` 验证 |
| E-04 | JRE/JDK 环境 | DataSync 要求 Java 运行时 | `java -version` |
| E-05 | SMTP 服务器可达性 | `smtp.metropolis.gov:25` | `telnet smtp.metropolis.gov 25` |
| E-06 | Socrata 平台可达性 | `https://data.metropolis.gov` | `curl -I https://data.metropolis.gov` |
| E-07 | DataSync 版本兼容性 | 仓库中为 v1.5.3，确认与 Socrata 平台 API 版本兼容 | 参考 Socrata DataSync 发布说明 |
| E-08 | 操作系统用户权限 | Java Preferences API 在 Linux 下写入 `~/.java/.userPrefs/`，需确认 ETL 运行用户一致 | `ls -la ~/.java/.userPrefs/` |

### 5.4 安全性检查

| 序号 | 检查项 | 说明 |
|------|--------|------|
| S-01 | `config.json` 不应出现在 Git 历史中 | `git log --all --diff-filter=A -- DataSync/config.json` |
| S-02 | `.gitignore` 应包含 `config.json` 或 `DataSync/config.json` | 防止意外提交真实凭据 |
| S-03 | `load_preferences.sh` 未硬编码任何凭据 | 目视确认脚本内容 |
| S-04 | .ktr 文件中命令拼接无注入风险 | 输入字段 (`file`, `datasetid` 等) 若来源于外部输入需验证；当前为 Kettle 内部 Mapping 传递，风险较低 |
| S-05 | DataSync 内部存储的凭据访问权限 | Java Preferences 文件权限应限制为 ETL 运行用户 |

---

## 6. 总结

本项目的 Kettle-to-DataSync 衔接设计遵循 **"一次加载凭据，多次无状态调用"** 的模式：

1. **凭据初始化** 通过 `config_template.json` + `load_preferences.sh` 完成，是一次性的手动操作
2. **数据发布** 通过三个 .ktr 子转换实现，作为 Kettle Mapping 嵌入父 ETL 作业中，通过 `ExecProcess` 步骤调用 `datasync.jar` 命令行
3. **三种变体** 分别对应标准 API、FTP 和 HTTP 三种上传通道，以适应不同数据量和网络环境

评审重点应放在：凭据安全（`config.json` 生命周期管理）、路径一致性（`DATA_SYNC_DIRECTORY` 配置）以及运行时环境依赖（Java、网络、用户权限）。
