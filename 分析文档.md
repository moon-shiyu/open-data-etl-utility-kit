# ETL 日志诊断工具 — 代码解析文档

## 一、工具总览

本仓库 `Log/` 目录下有四个 Shell 脚本，共同构成一套面向 Socrata 平台数据集 ETL 流程的运维诊断工具链。它们围绕"4x4 标识符"（形如 `abcd-1234` 的数据集 ID）展开，分别覆盖 **日志定位、运行时统计、当天概览、手动触发** 四个场景。

| 脚本 | 定位 | 典型使用场景 |
|---|---|---|
| `A_DatasetLogs.sh` | 按关键字检索日志文件 | 定位某个数据集的所有历史日志 |
| `A_ETLRuntimes.sh` | 提取运行耗时统计 | 排查性能退化、超时问题 |
| `A_TodayLogs.sh` | 列出当天生成的日志 | 每日巡检 / 快速确认今日作业状态 |
| `A_RunETL.sh` | 手动触发 ETL 作业 | 故障恢复 / 补跑失败的作业 |

---

## 二、逐脚本解析

### 2.1 A_DatasetLogs.sh — 数据集日志检索

```bash
if [ -z "$1" ]
then
   echo "Usage: A_DatasetLogs.sh StringToFindInLogName"
   exit 1
else
   ls -l | grep $1
   exit 0
fi
```

**功能**：在当前目录下按文件名关键字搜索日志文件。

**关键链路**：
- 接受一个字符串参数 `$1`（通常是数据集 4x4 ID 或名称片段）
- 通过 `ls -l | grep $1` 在当前目录长列表中做文本匹配
- 返回匹配行，包含文件大小、日期、文件名等信息

**排查价值**：管理员可以快速查看某个数据集产生了哪些日志文件、文件大小是否异常（空文件可能意味着作业未正常启动）、最后修改时间是否符合预期。

**注意事项**：
- `$1` 未加引号，含空格或特殊字符时会导致 glob 展开
- grep 没有 `-F` 标志，参数中的正则元字符（如 `.`）会被解释为通配符

---

### 2.2 A_ETLRuntimes.sh — 运行时统计

```bash
if [ -z "$1" ]
then
   echo "Usage: A_ETLRuntimes.sh StringToFindInLogName"
   exit 1
else
   cat *$1* | grep 'Processing ended after'
   exit 0
fi
```

**功能**：从匹配的日志文件中提取 ETL 处理耗时。

**关键链路**：
- `cat *$1*` — 使用通配符展开，读取所有文件名中包含 `$1` 的文件内容
- `grep 'Processing ended after'` — 过滤出包含处理结束耗时的行

**排查价值**：管理员可以纵向对比同一数据集多次运行的耗时，识别性能退化趋势。例如某数据集平时 30 秒完成但突然变成 10 分钟，可能意味着数据量暴增或上游接口变慢。

**日志格式假设**：ETL 程序在处理结束时会输出包含 `Processing ended after` 的行，通常后面跟着耗时数据（如秒数或 `HH:MM:SS` 格式）。

---

### 2.3 A_TodayLogs.sh — 当天日志概览

```bash
DATASETS_TO_EXCLUDE="n4j6-wkkf|t2qc-9pjd|qmqz-2xku|97wa-y6ff"

if [ -z "$1" ]
then
   ls -l | grep $(date +"%Y%m%d")
   exit 0
else
   case $1 in
      -e)
         ls -l | grep $(date +"%Y%m%d") | grep -Ev $DATASETS_TO_EXCLUDE
         exit 0
         ;;
      *)
         echo "Usage: A_Today.sh [-e]"
         echo "   -e Exclude a pre-specified set of datasets by 4x4..."
         exit 1
         ;;
   esac
fi
```

**功能**：列出当天产生的日志文件，可选排除高频数据集。

**关键链路**：
- `date +"%Y%m%d"` 生成当天日期字符串（如 `20260615`），假设日志文件名中包含日期
- 无参数：列出所有匹配今天日期的文件
- `-e` 参数：追加 `grep -Ev` 排除硬编码的 4 个高频数据集（`n4j6-wkkf`、`t2qc-9pjd`、`qmqz-2xku`、`97wa-y6ff`）

**排查价值**：每日巡检的入口工具。管理员可以一眼看到今天有哪些 ETL 任务已执行、哪些还未生成日志（即可能未触发）。`-e` 选项避免高频任务（如每小时运行的数据集）淹没输出。

**配置说明**：
- 排除列表 `DATASETS_TO_EXCLUDE` 是硬编码的，修改需直接编辑脚本
- 排除项之间用管道符 `|` 分隔，作为 `grep -E`（扩展正则）的 OR 模式

---

### 2.4 A_RunETL.sh — 手动触发 ETL

```bash
if [ -z "$1" ]
then
   echo "Usage: A_RunETL.sh Dataset4x4"
   exit 1
else
   # Sanity check
   if [ -z $(echo "$1" | grep "^[a-zA-Z0-9]\{4\}-[a-zA-Z0-9]\{4\}$") ]; then
      echo "Invalid Dataset4x4"
      exit 1
   fi
   CRONTAB_LINE="crontab -l | grep $1 | cut -d ' ' -f 6-"
   COMMAND_TO_RUN=$(eval $CRONTAB_LINE)
   eval $COMMAND_TO_RUN
   echo "Command executed: " $COMMAND_TO_RUN
   exit 0
fi
```

**功能**：根据 4x4 ID 从 crontab 中提取并立即执行对应的 ETL 命令。

**关键链路**：
1. **格式校验**：用正则 `^[a-zA-Z0-9]{4}-[a-zA-Z0-9]{4}$` 验证输入是否为合法的 4x4 标识符
2. **命令提取**：`crontab -l | grep $1 | cut -d ' ' -f 6-`
   - `crontab -l` 列出当前用户的定时任务
   - `grep $1` 找到包含该 4x4 的行
   - `cut -d ' ' -f 6-` 跳过前 5 个空格分隔的字段（即 cron 的 分/时/日/月/周 调度字段），取第 6 个字段及之后的所有内容，即实际执行的命令
3. **执行**：两次 `eval`——第一次执行管道命令提取结果，第二次执行提取出的 ETL 命令

**排查价值**：当定时任务失败或需要临时补跑时，管理员无需手动翻阅 crontab、拼命令，只需提供数据集 ID 即可触发。同时回显执行的命令便于审计。

**安全注意事项**：
- `eval` 的使用带有命令注入风险，但正则校验限制了输入只能是字母数字和连字符
- 如果 crontab 中有多行匹配同一个 4x4，`grep` 会返回多行，`cut` 拼接后可能产生非预期命令
- 变量 `$1` 在 `grep` 中未加引号，但因通过了正则校验，实际风险有限

---

## 三、四脚本协同排查流程

管理员在日常运维中可以将这四个工具串联使用，形成完整的诊断链路：

```
┌─────────────────────────────────────────────────────────────┐
│  1. A_TodayLogs.sh        → 巡检今日执行情况                │
│     发现缺失或异常的数据集                                  │
│          │                                                   │
│          ▼                                                   │
│  2. A_DatasetLogs.sh      → 检索该数据集的历史日志          │
│     确认是偶发还是持续性问题                                │
│          │                                                   │
│          ▼                                                   │
│  3. A_ETLRuntimes.sh      → 查看运行耗时趋势               │
│     判断是超时、性能退化还是根本未启动                      │
│          │                                                   │
│          ▼                                                   │
│  4. A_RunETL.sh           → 手动补跑失败的作业              │
│     从 crontab 提取命令并立即执行                           │
└─────────────────────────────────────────────────────────────┘
```

---

## 四、静态验证方法（无生产日志环境）

在没有生产环境日志的情况下，可以通过以下方式对脚本进行静态验证。

### 4.1 参数校验测试

直接在本地 Shell 中测试各脚本的参数处理逻辑：

```bash
# A_DatasetLogs.sh — 缺少参数应输出 Usage
bash Log/A_DatasetLogs.sh
# 预期输出: Usage: A_DatasetLogs.sh StringToFindInLogName
# 预期退出码: 1

# A_ETLRuntimes.sh — 缺少参数应输出 Usage
bash Log/A_ETLRuntimes.sh
# 预期输出: Usage: A_ETLRuntimes.sh StringToFindInLogName
# 预期退出码: 1

# A_TodayLogs.sh — 无效参数应输出 Usage
bash Log/A_TodayLogs.sh --invalid
# 预期输出: Usage: A_Today.sh [-e]

# A_RunETL.sh — 格式校验
bash Log/A_RunETL.sh "not-valid-format"
# 预期输出: Invalid Dataset4x4

bash Log/A_RunETL.sh "abcd-1234"
# 预期: 通过格式校验（在无 crontab 环境中后续步骤可能失败，但格式校验本身通过）

bash Log/A_RunETL.sh "ab!d-12@4"
# 预期输出: Invalid Dataset4x4（特殊字符被正则拒绝）
```

### 4.2 用示例日志文件模拟

创建模拟文件进行功能验证：

```bash
# 创建临时测试目录
mkdir -p /tmp/etl_test && cd /tmp/etl_test

# 模拟日志文件（文件名包含 4x4 ID 和日期）
touch "etl_abcd-1234_20260615_success.log"
touch "etl_abcd-1234_20260614_success.log"
touch "etl_wxyz-5678_20260615_error.log"
touch "etl_n4j6-wkkf_20260615_success.log"  # 高频数据集

# 向日志文件写入模拟内容
echo "Processing started at 2026-06-15 08:00:00" > etl_abcd-1234_20260615_success.log
echo "Processing ended after 45 seconds" >> etl_abcd-1234_20260615_success.log
echo "Processing started at 2026-06-14 08:00:00" > etl_abcd-1234_20260614_success.log
echo "Processing ended after 120 seconds" >> etl_abcd-1234_20260614_success.log
echo "Processing started at 2026-06-15 09:00:00" > etl_wxyz-5678_20260615_error.log
echo "ERROR: Connection timeout" >> etl_wxyz-5678_20260615_error.log
```

### 4.3 grep/cut/eval 用法验证

**grep 验证**：

```bash
# 测试 A_DatasetLogs.sh 的核心逻辑
ls -l | grep "abcd-1234"
# 预期: 列出两个 abcd-1234 相关的文件

# 测试 A_TodayLogs.sh 的日期过滤
ls -l | grep "20260615"
# 预期: 只列出今天的 3 个文件

# 测试 -e 排除功能
DATASETS_TO_EXCLUDE="n4j6-wkkf|t2qc-9pjd|qmqz-2xku|97wa-y6ff"
ls -l | grep "20260615" | grep -Ev "$DATASETS_TO_EXCLUDE"
# 预期: 排除 n4j6-wkkf 后只剩 2 个文件
```

**cat + grep 验证**：

```bash
# 测试 A_ETLRuntimes.sh 的核心逻辑
cat *abcd-1234* | grep 'Processing ended after'
# 预期输出:
#   Processing ended after 45 seconds
#   Processing ended after 120 seconds
```

**正则校验验证**：

```bash
# 测试 A_RunETL.sh 的 4x4 校验正则
echo "abcd-1234" | grep "^[a-zA-Z0-9]\{4\}-[a-zA-Z0-9]\{4\}$"
# 预期: 输出 abcd-1234（匹配成功）

echo "abc-12345" | grep "^[a-zA-Z0-9]\{4\}-[a-zA-Z0-9]\{4\}$"
# 预期: 无输出（不匹配，长度不对）

echo "ab!d-12@4" | grep "^[a-zA-Z0-9]\{4\}-[a-zA-Z0-9]\{4\}$"
# 预期: 无输出（不匹配，含特殊字符）
```

**cut + eval 验证**：

```bash
# 模拟 crontab 行来验证 cut 的字段提取
echo "0 8 * * * /opt/etl/run.sh abcd-1234 --full" | cut -d ' ' -f 6-
# 预期输出: /opt/etl/run.sh abcd-1234 --full
# 解释: 跳过前 5 个字段（cron 调度部分），返回实际命令

# 验证 eval 的两层执行
CRONTAB_LINE="echo '0 8 * * * /opt/etl/run.sh abcd-1234' | cut -d ' ' -f 6-"
COMMAND_TO_RUN=$(eval $CRONTAB_LINE)
echo "Extracted command: $COMMAND_TO_RUN"
# 预期: Extracted command: /opt/etl/run.sh abcd-1234
```

### 4.4 清理测试环境

```bash
rm -rf /tmp/etl_test
```

---

## 五、外部环境依赖

| 依赖项 | 涉及脚本 | 说明 |
|---|---|---|
| Bash Shell | 全部 | 所有脚本为 Bash 脚本 |
| `ls`, `grep`, `cat`, `cut` | 全部 | 标准 Unix 命令行工具 |
| `date` | A_TodayLogs.sh | 需要支持 `+"%Y%m%d"` 格式化 |
| `eval` | A_RunETL.sh | 用于动态执行提取的命令 |
| `crontab` | A_RunETL.sh | 需要当前用户有 crontab 配置，且包含 4x4 ID |
| 日志文件命名约定 | A_DatasetLogs.sh, A_TodayLogs.sh | 文件名需包含 4x4 ID 和 `YYYYMMDD` 日期格式 |
| 日志内容格式 | A_ETLRuntimes.sh | 日志中需包含 `Processing ended after` 文本 |
| 工作目录 | 全部 | 脚本假设在日志文件所在目录中执行（`ls -l` 和 `cat *$1*` 均针对当前目录） |

---

## 六、总结

这四个脚本虽然各自只有不到 20 行，但它们覆盖了 ETL 运维中从**发现问题**到**解决问题**的完整闭环：用 `A_TodayLogs.sh` 做日常巡检发现异常，用 `A_DatasetLogs.sh` 追溯历史，用 `A_ETLRuntimes.sh` 分析耗时趋势，用 `A_RunETL.sh` 补跑失败任务。这种"Unix 哲学"式的小工具组合模式使得每个脚本职责单一、易于理解和维护。
