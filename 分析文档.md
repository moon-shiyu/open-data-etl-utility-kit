# Socrata DataSync 上传配置 — 代码解析报告

## 一、项目概览

本项目通过 Pentaho Kettle (PDI) ETL 管道生成数据文件，再调用 Socrata DataSync CLI 工具将文件发布到 Socrata 开放数据平台。整体架构分为三层：

1. **凭据初始化层** — `DataSync/config_template.json` + `DataSync/load_preferences.sh`
2. **ETL 执行层** — Kettle 主作业（上游，不在本次分析范围）
3. **数据上传层** — `ETL/Utilities/DataSync*.ktr`（三个子转换变体）

---

## 二、各模块职责详解

### 2.1 `DataSync/config_template.json` — 配置模板

该文件是 Socrata DataSync 的 **偏好配置模板**，包含连接到 Socrata 实例所需的全部身份认证和运行时参数：

| 配置项 | 值（模板） | 用途 |
|---|---|---|
| `domain` | `https://data.metropolis.gov` | Socrata 目标实例域名 |
| `username` | 含防误操作方括号后缀 | Socrata 登录账号，方括号内容防止误用模板直接运行导致账号锁定 |
| `password` | `[Redacted...]` | Socrata 登录密码 |
| `appToken` | `[Redacted...]` | Socrata API 应用令牌 |
| `adminEmail` | `clark.kent@metropolis.gov` | 错误通知收件人 |
| `emailUponError` | `true` | 发生错误时是否发送邮件 |
| `logDatasetID` | `abcd-1234` | DataSync 运行日志写入的 Socrata 数据集 ID |
| `outgoingMailServer` | `smtp.metropolis.gov` | SMTP 邮件服务器 |
| `smtpPort` | `25` | SMTP 端口 |
| `sslPort` / `smtpUsername` / `smtpPassword` | 空字符串 | 可选的 SMTP SSL 与认证配置 |
| `filesizeChunkingCutoffMB` | `10` | 超过 10MB 的文件启用分块上传 |
| `numRowsPerChunk` | `50000` | 每块包含的最大行数 |

**设计要点**：模板中 `username` 字段刻意附加了方括号说明文字，作为安全保护机制——若运维人员未正确编辑即运行脚本，该用户名将因格式非法而认证失败，从而避免因错误密码反复重试导致 Socrata 账号被锁定。

### 2.2 `DataSync/load_preferences.sh` — 偏好加载脚本

```bash
java -jar datasync.jar --config config.json --jobType LoadPreferences
```

该脚本执行 **一次性** 的凭据导入操作，流程为：

1. 运维人员将 `config_template.json` 复制为 `config.json`
2. 在 `config.json` 中填入真实的 `username`、`password`、`appToken` 等敏感信息
3. 运行本脚本，DataSync 将凭据写入其内部持久化存储（DataSync "memory"，即 Java Preferences 或本地文件）
4. **删除 `config.json`**，确保明文凭据不驻留磁盘

此后 DataSync CLI 在被 Kettle 子转换调用时可直接使用已加载的凭据，无需每次传入 `config.json`。

### 2.3 `ETL/Utilities/DataSync.ktr` — 标准上传子转换

这是最通用的 DataSync 调用变体，作为 Kettle **Mapping（子转换）** 被上游作业嵌入调用。

**数据流步骤**：

```
Set Variable Data_Sync_ETL (=Y)
    ↓
Mapping input specification
    ↓  接收字段: file, header, datasetid, method
DataSync Directory
    ↓  读取 ${DATA_SYNC_DIRECTORY}
Build DataSync Command (Formula 步骤)
    ↓  拼接命令行:
    ↓  java -jar {dir}datasync.jar --fileToPublish {file}
    ↓    --fileToPublishHasHeaderRow {header}
    ↓    --datasetID {datasetid} --publishMethod {method}
Run DataSync Job (ExecProcess)
    ↓  捕获 stdout / stderr / exitValue
    ├→ Mapping output specification（返回上游）
    └→ Write to log - DS Results（写入 Kettle 日志）
```

**映射输入字段**：

| 字段 | 说明 |
|---|---|
| `file` | ETL 输出的 CSV/TSV 文件完整路径 |
| `header` | 文件是否包含表头行（`true`/`false`） |
| `datasetid` | 目标 Socrata 数据集的四段式 ID（如 `abcd-1234`） |
| `method` | 发布方法：`replace`（全量替换）、`upsert`（增量更新）等 |

**关键特征**：
- 发布方法由上游动态传入，灵活度最高
- 不使用控制文件（control file）
- 不指定传输协议，使用 DataSync 默认行为

### 2.4 `ETL/Utilities/DataSyncFTP.ktr` — FTP 传输变体

**与标准变体的差异**：

| 方面 | DataSync.ktr | DataSyncFTP.ktr |
|---|---|---|
| 映射输入 | `file, header, datasetid, method` | `file, datasetid, controlfile` |
| 发布方法 | 上游传入（动态） | 硬编码为 `replace` |
| 传输协议 | 默认 | `--publishViaFTP true` |
| 控制文件 | 无 | `--pathToFTPControlFile {controlfile}` |
| 表头参数 | `--fileToPublishHasHeaderRow` | 无（由控制文件定义） |

拼接的命令行：
```
java -jar {dir}datasync.jar --fileToPublish {file}
  --datasetID {datasetid} --publishMethod replace
  --publishViaFTP true --pathToFTPControlFile {controlfile}
```

FTP 变体适用于 **大文件场景**：通过 Socrata 的 FTP SmartUpdate 管道绕过 HTTP API 的负载限制，配合控制文件定义列映射和数据类型。

### 2.5 `ETL/Utilities/DataSyncHTTP.ktr` — HTTP 传输变体

**与 FTP 变体的差异**：

| 方面 | DataSyncFTP.ktr | DataSyncHTTP.ktr |
|---|---|---|
| 传输协议标志 | `--publishViaFTP true` | `--publishViaHttp true` |
| 控制文件参数名 | `--pathToFTPControlFile` | `--pathToControlFile` |
| JVM 内存 | 默认 | `-Xmx1g`（最大堆内存 1GB） |

拼接的命令行：
```
java -jar -Xmx1g {dir}datasync.jar --fileToPublish {file}
  --datasetID {datasetid} --publishMethod replace
  --publishViaHttp true --pathToControlFile {controlfile}
```

HTTP 变体显式分配了 1GB JVM 堆内存，表明此模式预期处理较大数据集，需要更多内存进行 HTTP 分块上传。

---

## 三、关键衔接链路

### 3.1 凭据链路

```
config_template.json
    → (手动复制/编辑) → config.json
        → load_preferences.sh → DataSync 内部持久存储
            → DataSync*.ktr 运行时自动读取（无需再传 config）
```

三个 `.ktr` 子转换的注释明确说明：
> "This subtransformation assumes the user ID, password, API key, and domain are already set in DataSync memory."

### 3.2 文件路径链路

```
kettle.properties
    → ${DATA_SYNC_DIRECTORY}
        → GetVariable 步骤读取
            → Formula 步骤拼接为 "{dir}datasync.jar" 的完整路径
```

`DATA_SYNC_DIRECTORY` 的值必须以 `/` 结尾，否则拼接后路径缺少分隔符（如 `/opt/datasyncdata sync.jar` 而非 `/opt/datasync/datasync.jar`）。

### 3.3 ETL 数据链路

```
上游 Kettle 作业
    → 生成 CSV/TSV 数据文件
    → 组装 file / datasetid / method (或 controlfile) 字段
    → 通过 Mapping 调用 DataSync*.ktr 子转换
        → 子转换拼接命令行 → ExecProcess 执行 DataSync CLI
            → Socrata 平台
```

变量 `Data_Sync_ETL` 在子转换入口被设为 `Y`（作用域 `ROOT_JOB`），供上游作业判断当前执行路径是否包含 DataSync 步骤。

---

## 四、三个变体的选型指南

| 场景 | 推荐变体 | 理由 |
|---|---|---|
| 小型数据集、需灵活指定发布方法 | `DataSync.ktr` | 映射输入最灵活，可选 replace/upsert |
| 大文件、需 FTP 分块上传 | `DataSyncFTP.ktr` | 使用 Socrata FTP SmartUpdate 管道 |
| 大文件、需 HTTP 分块上传 | `DataSyncHTTP.ktr` | 增加 JVM 内存，HTTP 协议更易穿越防火墙 |
| 需要列映射 / 数据类型控制 | FTP 或 HTTP 变体 | 依赖控制文件（control file）定义 schema |

---

## 五、代码评审验证清单

以下为面向代码评审的静态验证说明，列出需逐项检查的配置项、文件路径与外部依赖。

### 5.1 文件存在性与命名检查

| 检查项 | 预期 | 验证方法 |
|---|---|---|
| `DataSync/datasync.jar` 是否存在 | 仓库内仅有 `DataSync-1.5.3.jar - Rename to datasync.jar` 提示文件 | 确认部署时已将 jar 重命名为 `datasync.jar` |
| `kettle.properties` 中 `DATA_SYNC_DIRECTORY` | 已定义且以 `/` 结尾 | 检查部署环境的 `~/.kettle/kettle.properties` |
| `config.json` 不应存在于仓库 | 不应被提交 | 检查 `.gitignore` 是否包含 `config.json` |
| 控制文件（control file）路径有效性 | FTP/HTTP 变体依赖上游传入的 `controlfile` 字段 | 确认上游作业传入的路径指向真实存在的控制文件 |

### 5.2 凭据与安全检查

| 检查项 | 风险等级 | 说明 |
|---|---|---|
| `config_template.json` 中无真实密码 | **高** | 模板中 `password` 和 `appToken` 必须保持 `[Redacted...]` 占位符 |
| `config.json` 不在版本控制中 | **高** | 含明文凭据的 `config.json` 绝不可提交到 Git |
| DataSync 内部存储的凭据权限 | **中** | Java Preferences 或本地文件应仅对运行用户可读 |
| `load_preferences.sh` 执行后删除 `config.json` | **高** | 脚本注释要求手动删除，建议在脚本末尾增加自动删除逻辑 |
| `username` 字段的安全后缀是否已移除 | **中** | 生产 `config.json` 中必须移除方括号说明文字，否则认证必定失败 |

### 5.3 配置一致性检查

| 检查项 | 说明 |
|---|---|
| `domain` 是否指向正确的 Socrata 实例 | 模板使用虚构域名 `data.metropolis.gov`，生产环境必须替换 |
| `logDatasetID` 是否为有效的 Socrata 数据集 | 模板值 `abcd-1234` 为占位符，需替换为真实的日志数据集 ID |
| `adminEmail` 是否为有效收件地址 | 确保错误通知能送达正确的运维人员 |
| `outgoingMailServer` / `smtpPort` 是否可达 | 从部署服务器验证 SMTP 连通性 |
| `filesizeChunkingCutoffMB` 与 `numRowsPerChunk` 的合理性 | 10MB / 50000 行为默认值，根据实际数据量调优 |

### 5.4 子转换命令行拼接检查

| 检查项 | 涉及文件 | 说明 |
|---|---|---|
| `DataSyncDirectory` 末尾斜杠 | 三个 `.ktr` | Formula 直接拼接 `[DataSyncDirectory] & "datasync.jar"`，若变量不以 `/` 结尾将导致路径错误 |
| FTP 变体硬编码 `replace` | `DataSyncFTP.ktr:115` | 确认 FTP 场景是否始终需要全量替换，若有 upsert 需求则此硬编码不满足 |
| HTTP 变体硬编码 `replace` | `DataSyncHTTP.ktr:115` | 同上 |
| HTTP 变体 `-Xmx1g` 是否足够 | `DataSyncHTTP.ktr:115` | 对于超大数据集可能需要增大至 `-Xmx2g` |
| `--publishViaFTP` vs `--publishViaHttp` 拼写 | FTP/HTTP `.ktr` | 确认与当前 DataSync 版本（1.5.3）的 CLI 参数名一致 |
| `--pathToFTPControlFile` vs `--pathToControlFile` | FTP/HTTP `.ktr` | 两个变体使用不同的参数名，确认与 DataSync CLI 文档一致 |
| `failwhennotsuccess` 设为 `N` | 三个 `.ktr` | ExecProcess 步骤不会因 DataSync 失败而中止转换，需确认上游作业是否有其他错误处理逻辑 |

### 5.5 外部环境依赖清单

| 依赖项 | 类型 | 说明 |
|---|---|---|
| Java Runtime (JRE/JDK) | 运行时 | DataSync 是 Java 应用，需要 `java` 命令在 `$PATH` 中可用 |
| `datasync.jar` (v1.5.3) | 运行时 | 需从仓库提示文件重命名或从 Socrata 官方下载 |
| Socrata 平台账号与 API Token | 凭据 | `username` + `password` + `appToken` |
| SMTP 邮件服务器 | 网络 | 错误通知所需，`smtp.metropolis.gov:25` |
| Socrata 日志数据集 | 平台配置 | `logDatasetID` 指向的数据集必须已创建且有写入权限 |
| `kettle.properties` | Kettle 配置 | 必须包含 `DATA_SYNC_DIRECTORY` 变量定义 |
| 控制文件（FTP/HTTP 模式） | 数据定义 | JSON 格式，定义列映射和数据类型，路径由上游作业传入 |
| 网络连通性 | 网络 | 部署服务器必须能访问 Socrata 域名（HTTPS 443）及 FTP 端口（若使用 FTP 模式） |

---

## 六、总结

该项目采用**凭据预加载 + 子转换复用**的架构模式：

- 凭据通过 `load_preferences.sh` 一次性加载到 DataSync 内部存储，避免在 ETL 作业中传递明文密码
- 三个 `.ktr` 子转换通过 Kettle Mapping 机制被上游作业嵌入调用，共享相同的执行骨架（设变量 → 接收输入 → 读目录 → 拼命令 → 执行 → 记日志），仅在命令行参数上有所区别
- 标准变体灵活、FTP/HTTP 变体面向大文件并强制使用 `replace` 模式

主要风险点集中在**凭据管理**（config.json 生命周期）和**路径拼接**（DATA_SYNC_DIRECTORY 末尾斜杠），代码评审时应重点关注这两个方面。
