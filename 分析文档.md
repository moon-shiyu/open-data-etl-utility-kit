# Socrata DataSync 上传配置 -- 代码解析文档

## 一、项目概览

本项目是一个基于 **Pentaho Kettle (PDI)** 的 ETL 工具包，用于将数据经过 ETL 处理后，通过 **Socrata DataSync** 工具发布到 Socrata 开放数据平台。整体架构分为两层：

- **Kettle ETL 层**：负责数据抽取、转换，并输出为 CSV 等平面文件。
- **DataSync 层**：通过 Java CLI 调用 `datasync.jar`，将 ETL 产出的文件发布到 Socrata 门户。

两层之间的衔接通过 **Shell 命令行参数** 实现：Kettle 在子转换（sub-transformation）中动态拼装出 `java -jar datasync.jar ...` 命令，再通过 `ExecProcess` 步骤执行。

---

## 二、文件清单与职责

| 文件路径 | 类型 | 职责 |
|----------|------|------|
| `DataSync/config_template.json` | JSON 模板 | DataSync 全局偏好配置模板，包含 Socrata 域名、凭据、SMTP 邮件通知、分块参数等 |
| `DataSync/load_preferences.sh` | Shell 脚本 | 一次性工具脚本，用于将 `config.json` 中的偏好加载到 DataSync 的"记忆"（Java Preferences）中 |
| `DataSync/DataSync-1.5.3.jar` | JAR 包 | Socrata DataSync CLI 工具（需重命名为 `datasync.jar`） |
| `ETL/Utilities/DataSync.ktr` | Kettle 子转换 | 通用 DataSync 发布 -- 使用默认传输方式，支持 `header`/`method` 参数化 |
| `ETL/Utilities/DataSyncFTP.ktr` | Kettle 子转换 | FTP 传输变体 -- 强制 `--publishViaFTP true`，固定 `--publishMethod replace` |
| `ETL/Utilities/DataSyncHTTP.ktr` | Kettle 子转换 | HTTP 传输变体 -- 强制 `--publishViaHttp true`，固定 `--publishMethod replace`，额外设置 `-Xmx1g` JVM 内存 |

---

## 三、关键链路：ETL 输出如何衔接到 DataSync

### 3.1 完整调用链

```
Shell 入口 (Setup-Script.sh / ETL_Setup.sh)
  |
  v
kitchen.sh Standard_ETL.kjb
  |  参数: P_Data_Sync_ETL, P_ETL_Transformation_Name, P_ETL_Transformation_Directory
  v
${ETL_DIRECTORY}${P_ETL_Transformation_Directory}${P_ETL_Transformation_Name}.ktr
  (通常为 ETL_Template.ktr)
  |
  +---> Prepare_for_DataSyncSSync.ktr    (准备控制文件)
  |
  +---> DataSyncHTTP.ktr                 (通过 HTTP 发布到 Socrata)
         |
         v
       动态拼装命令:
         java -jar -Xmx1g <DATA_SYNC_DIRECTORY>datasync.jar
           --fileToPublish <file>
           --datasetID <datasetid>
           --publishMethod replace
           --publishViaHttp true
           --pathToControlFile <controlfile>
         |
         v
       ExecProcess 步骤执行命令，捕获 stdout/stderr/exitValue
         |
         v
       WriteToLog 步骤记录结果
```

### 3.2 三个 KTR 变体的差异对比

| 特征 | DataSync.ktr | DataSyncFTP.ktr | DataSyncHTTP.ktr |
|------|-------------|-----------------|------------------|
| **Mapping 输入字段** | `file`, `header`, `datasetid`, `method` | `file`, `datasetid`, `controlfile` | `file`, `datasetid`, `controlfile` |
| **publishMethod** | 参数化 `[method]` | 固定 `replace` | 固定 `replace` |
| **传输协议** | 默认（由 DataSync 偏好决定） | `--publishViaFTP true` | `--publishViaHttp true` |
| **FTP 控制文件** | 无 | `--pathToFTPControlFile [controlfile]` | 无 |
| **HTTP 控制文件** | 无 | 无 | `--pathToControlFile [controlfile]` |
| **fileToPublishHasHeaderRow** | 有 `[header]` 参数 | 无 | 无 |
| **JVM 参数** | 无 | 无 | `-Xmx1g` |
| **当前被引用状态** | 未被任何父转换引用（孤立） | 未被任何父转换引用（孤立） | **被 `ETL_Template.ktr` 引用** |

### 3.3 子转换内部步骤流（三个变体共享同一结构）

```
Set Variable Data_Sync_ETL          -- 设置 ROOT_JOB 级变量 Data_Sync_ETL=Y
        |
Mapping input specification         -- 接收父转换传入的字段（file, datasetid 等）
        |
DataSync Directory                  -- 从 kettle.properties 读取 ${DATA_SYNC_DIRECTORY}
        |
Build DataSync Command              -- 用 Formula 步骤拼接完整 CLI 命令
        |
Run DataSync Job                    -- ExecProcess 执行命令，输出三个字段:
        |                               DataSyncResultOutput / DataSyncErrorOutput / DataSyncExitValue
        |
Mapping output specification        -- 将结果返回父转换
```

### 3.4 config_template.json 的作用

`config_template.json` **不参与 KTR 子转换的运行**（子转换注释明确声明"不使用 config.json"）。它的用途是：

1. 作为一次性初始化模板，用户将其复制为 `config.json` 并填入真实凭据。
2. 通过 `load_preferences.sh` 执行 `java -jar datasync.jar --config config.json --jobType LoadPreferences`，将配置写入 Java Preferences 存储（DataSync 的"记忆"）。
3. 完成后删除 `config.json` 以确保安全。
4. 此后所有 DataSync CLI 调用自动从 Java Preferences 读取域名、用户名、密码、appToken 等信息，无需再传 `--config` 参数。

---

## 四、config_template.json 配置项详解

| 配置项 | 说明 | 示例值 |
|--------|------|--------|
| `domain` | Socrata 开放数据门户的域名 | `https://data.metropolis.gov` |
| `username` | Socrata 账号（带防误操作注释） | `superman@metropolis.gov[...]` |
| `password` | Socrata 密码（需手动填入） | `[Redacted]` |
| `appToken` | Socrata 应用 API Token（需手动填入） | `[Redacted]` |
| `adminEmail` | 管理员邮箱 | `clark.kent@metropolis.gov` |
| `emailUponError` | 出错时是否发送邮件通知 | `true` |
| `logDatasetID` | DataSync 日志写入的 Socrata 数据集 4x4 ID | `abcd-1234` |
| `outgoingMailServer` | SMTP 邮件服务器地址 | `smtp.metropolis.gov` |
| `smtpPort` | SMTP 端口 | `25` |
| `sslPort` | SSL 端口（可选） | `""` |
| `smtpUsername` | SMTP 用户名（可选） | `""` |
| `smtpPassword` | SMTP 密码（可选） | `""` |
| `filesizeChunkingCutoffMB` | 文件分块阈值（MB） | `10` |
| `numRowsPerChunk` | 每块行数 | `50000` |

---

## 五、外部依赖与环境配置

### 5.1 kettle.properties（必须手动创建）

位置：
- Linux/Mac: `~/.kettle/kettle.properties`
- Windows: `%USERPROFILE%\.kettle\kettle.properties`

必须包含以下键值：

```properties
ETL_DIRECTORY = /path/to/open-data-etl-utility-kit/
DATA_SYNC_DIRECTORY = /path/to/open-data-etl-utility-kit/DataSync/
```

> `DATA_SYNC_DIRECTORY` 的值必须以路径分隔符结尾（`/` 或 `\`），因为 KTR 中的公式直接拼接为 `<DataSyncDirectory>datasync.jar`。

### 5.2 运行时依赖

| 依赖项 | 说明 |
|--------|------|
| **Java Runtime** | DataSync 是 Java JAR，需要 JRE 环境；`java` 必须在 PATH 中 |
| **datasync.jar** | 仓库中为 `DataSync-1.5.3.jar`，使用前需重命名为 `datasync.jar` |
| **Pentaho Kettle (PDI)** | 需要 kitchen.sh / spoon.sh 来运行 `.kjb` 和 `.ktr` 文件 |
| **网络访问** | 需要能访问 Socrata 域名的 HTTPS 端口（HTTP 变体）或 FTP 端口（FTP 变体） |
| **SMTP 服务器** | 如果 `emailUponError=true`，需要可达的 SMTP 服务器 |

### 5.3 凭据依赖

| 凭据 | 存储位置 | 用途 |
|------|----------|------|
| Socrata 用户名/密码 | DataSync Java Preferences（通过 load_preferences.sh 加载） | 认证发布 |
| Socrata App Token | DataSync Java Preferences | API 速率限制与追踪 |
| SMTP 用户名/密码 | DataSync Java Preferences | 错误通知邮件 |

---

## 六、代码评审验证清单

以下检查项面向代码评审者，**不修改代码**，仅做静态验证。

### 6.1 文件路径与引用完整性

- [ ] `ETL/Utilities/DataSync.ktr` 和 `ETL/Utilities/DataSyncFTP.ktr` 当前**未被任何父转换/作业引用**，确认它们是否为遗留文件或有计划集成。
- [ ] `ETL/Utilities/Prepare_for_DataSyncFTP.ktr` 和 `ETL/Utilities/Prepare_for_DataSyncHTTP.ktr` 同样未被引用，确认是否需要清理。
- [ ] `ETL_Template.ktr` 中引用 `DataSyncHTTP.ktr` 的路径为 `${ETL_DIRECTORY}Utilities/DataSyncHTTP.ktr`，确认 `${ETL_DIRECTORY}` 变量在实际部署环境中正确设置且路径可达。
- [ ] 三个 KTR 文件内部 `<name>` 标签与实际文件名不完全一致（`DataSync.ktr` 内部 name 为 `DataSyncFTP`，`DataSyncFTP.ktr` 内部 name 为 `DataSync`），虽然不影响功能，但可能造成日志混淆。

### 6.2 配置项检查

- [ ] `config_template.json` 中的 `domain`、`username`、`adminEmail`、`logDatasetID` 均为示例值，确认部署前已替换为真实值。
- [ ] `username` 字段中包含方括号注释作为防误操作机制，确认用户在填入真实凭据后已移除该注释部分。
- [ ] `password` 和 `appToken` 标记为 `[Redacted]`，确认不会以明文形式提交到版本控制。
- [ ] `config.json`（运行时生成的临时文件）确认已被加入 `.gitignore`，或确认从未出现在版本历史中。

### 6.3 安全相关

- [ ] `load_preferences.sh` 注释要求在运行后"删除 config.json"，确认运维流程中有对应的清理步骤或自动化。
- [ ] 仓库中**不存在** `config.json` 文件（已验证），确认凭据未泄露。
- [ ] 确认 `.gitignore` 中包含 `config.json` 模式，防止未来误提交。
- [ ] KTR 文件中注释声明"不使用 config.json"，凭据完全依赖 DataSync Java Preferences 存储，这降低了凭据泄露风险但需要确认部署环境的 Preferences 已正确初始化。

### 6.4 命令拼装安全性

- [ ] `DataSync.ktr` 中的公式允许 `method` 和 `header` 参数化传入，检查父调用方是否对这些字段做了合法性校验（如仅允许 `replace`/`append`/`upsert`）。
- [ ] 所有变体中的 `file`、`datasetid`、`controlfile` 字段直接拼接到 shell 命令中，确认上游不会注入特殊字符（命令注入风险）。
- [ ] `ExecProcess` 步骤的 `failwhennotsuccess` 设为 `N`（不自动失败），确认父转换中有对应的退出码检查逻辑。

### 6.5 功能一致性

- [ ] `DataSyncFTP.ktr` 使用 `--pathToFTPControlFile`，`DataSyncHTTP.ktr` 使用 `--pathToControlFile`，两者参数名不同，确认这是 Socrata DataSync CLI 的预期接口差异。
- [ ] `DataSyncHTTP.ktr` 额外指定了 `-Xmx1g`（JVM 最大堆内存 1GB），确认运行环境有足够的内存资源。
- [ ] 三个变体均将 `Data_Sync_ETL` 设置为 ROOT_JOB 级变量，确认 `Standard_ETL.kjb` 中对此变量的消费逻辑正确。

---

## 七、架构总结

```
                    +-------------------+
                    |  kettle.properties |
                    |  (DATA_SYNC_DIR,  |
                    |   ETL_DIRECTORY)   |
                    +--------+----------+
                             |
                             v
+----------+    +------------------------+    +---------------------+
|  Shell   |--->|   Standard_ETL.kjb     |--->|  ETL_Template.ktr   |
|  Script  |    |   (参数化调度)          |    |  (数据转换 + 发布)   |
+----------+    +------------------------+    +----------+----------+
                                                       |
                                          +------------+------------+
                                          |                         |
                               Prepare_for_DataSync     DataSyncHTTP.ktr
                                          |                         |
                                          |                +--------+--------+
                                          |                | 拼接 CLI 命令    |
                                          |                | ExecProcess 执行 |
                                          |                +--------+--------+
                                          |                         |
                                          |                         v
                                    +-----------+    +------------------------+
                                    | config_   |--->|  DataSync Java         |
                                    | template  |    |  Preferences           |
                                    | .json     |    |  (域名/凭据/SMTP)      |
                                    +-----------+    +------------------------+
                                          |
                                   load_preferences.sh
                                   (一次性加载到 Preferences)
```

核心设计思想：
1. **凭据与逻辑分离**：敏感信息通过 Java Preferences 存储，KTR 文件中不硬编码任何凭据。
2. **参数化调度**：`Standard_ETL.kjb` 通过参数动态选择要执行的转换，支持多种数据发布场景。
3. **传输协议可替换**：三个 DataSync 变体共享相同的步骤结构，仅在命令拼装公式中区分传输协议和参数。
4. **子转换复用**：DataSync KTR 设计为 Kettle Mapping 子转换，通过 MappingInput/MappingOutput 与父转换交换数据。
