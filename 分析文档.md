# Socrata DataSync 上传配置 -- 代码解析文档

> **项目**: Open Data ETL Utility Kit (City of Chicago)
> **分析范围**: `DataSync/config_template.json`、`DataSync/load_preferences.sh`、`ETL/Utilities/DataSync*.ktr` 及相关上下游文件
> **分析日期**: 2026-06-16
> **任务类型**: code-explanation（代码解析，不修改代码）

---

## 一、项目总览

本项目是一个基于 **Pentaho/Kettle** 的开源 ETL 框架，用于将数据从关系型数据库或其他数据源抽取、转换后，通过 **Socrata DataSync** Java 工具发布到 Socrata 开放数据门户（如 `data.cityofchicago.org`）。

核心设计理念：**凭据一次性加载，运行时免密调用**。即在部署阶段通过 `load_preferences.sh` 将 Socrata 凭据写入 DataSync 的 Java Preferences 存储中，此后 Kettle 子转换调用 `datasync.jar` 时无需再传递用户名/密码/AppToken。

---

## 二、关键文件清单与职责

| 文件路径 | 类型 | 职责 |
|---|---|---|
| `DataSync/config_template.json` | JSON 模板 | DataSync 无头模式（headless）凭据配置模板 |
| `DataSync/load_preferences.sh` | Shell 脚本 | 调用 `datasync.jar --jobType LoadPreferences` 将 config.json 中的凭据写入 Java Preferences |
| `DataSync/DataSync-1.5.3.jar - Rename to datasync.jar` | JAR 二进制 | Socrata DataSync v1.5.3 应用程序本体（需手动重命名） |
| `ETL/Utilities/DataSync.ktr` | Kettle 转换 | **通用型** DataSync 子转换 -- 通过 SODA API（直接 HTTP）发布 |
| `ETL/Utilities/DataSyncFTP.ktr` | Kettle 转换 | **FTP 型** DataSync 子转换 -- 通过 FTP 协议发布 |
| `ETL/Utilities/DataSyncHTTP.ktr` | Kettle 转换 | **HTTP 型** DataSync 子转换 -- 通过 HTTP 协议发布 |
| `ETL/Utilities/Prepare_for_DataSyncFTP.ktr` | Kettle 转换 | 路径预处理 -- 剥离 `file://` / `file:///` 前缀（FTP 变体） |
| `ETL/Utilities/Prepare_for_DataSyncHTTP.ktr` | Kettle 转换 | 路径预处理 -- 剥离 `file://` / `file:///` 前缀（HTTP 变体） |
| `ETL/Utilities/Prepare_for_DataSyncSSync.ktr` | Kettle 转换 | 路径预处理 -- 剥离 `file://` / `file:///` 前缀（SSync 变体） |
| `ETL/Utilities/ETL_Completion_E-Mail.ktr` | Kettle 转换 | ETL 完成后发送结果邮件（含 DataSync 执行结果） |
| `ETL/Utilities/ETL_Specific_To_Addresses.ktr` | Kettle 转换 | 读取各 ETL 专属收件人列表 |
| `ETL/Standard_ETL.kjb` | Kettle 作业 | **主编排作业** -- 串联变量设置、ETL 执行、邮件通知 |
| `ETL/ETL_Template.ktr` | Kettle 转换 | 新建 ETL 的模板，展示完整 DataSync HTTP 工作流 |
| `ETL/Standard_ETL.sh` | Shell 模板 | ETL 启动器模板（source ETL_Setup.sh 后调用 Setup-Script.sh） |
| `ETL/Setup-Script.sh` | Shell 脚本 | 实际调用 `kitchen.sh` 执行 `Standard_ETL.kjb` 的脚本 |
| `ETL/ETL_Setup.sh` | Shell 脚本 | 设置环境变量（NOW、PATH、ETL_ROOT_DIR） |
| `ETL/_control.json` | JSON 模板 | DataSync 控制文件模板（定义 CSV 解析规则和数据集操作类型） |
| `.gitignore` | Git 配置 | 排除 `DataSync/datasync.jar` 等敏感/二进制文件 |

---

## 三、ETL 与 DataSync 衔接机制详解

### 3.1 凭据初始化链路（部署阶段，一次性）

```
config_template.json  -->  [用户手动复制并填写]  -->  config.json
                                                          |
                                                          v
                                                  load_preferences.sh
                                                          |
                                                          v
                                    java -jar datasync.jar --config config.json
                                                 --jobType LoadPreferences
                                                          |
                                                          v
                                            DataSync Java Preferences Store
                                            (持久化存储 domain/user/pass/appToken)
                                                          |
                                                          v
                                                  [用户删除 config.json]
```

**config_template.json 字段说明**:

| 字段 | 示例值 | 用途 |
|---|---|---|
| `domain` | `https://data.metropolis.gov` | Socrata 门户域名 |
| `username` | `superman@metropolis.gov` | Socrata 登录邮箱（模板含防锁号提示） |
| `password` | `[Redacted]` | Socrata 账户密码 |
| `appToken` | `[Redacted]` | Socrata API 应用令牌 |
| `adminEmail` | `clark.kent@metropolis.gov` | 管理员联系邮箱 |
| `emailUponError` | `"true"` | 出错时是否发邮件 |
| `logDatasetID` | `abcd-1234` | DataSync 日志数据集的 4x4 ID |
| `outgoingMailServer` | `smtp.metropolis.gov` | SMTP 服务器地址 |
| `smtpPort` | `"25"` | SMTP 端口 |
| `sslPort` | `""` | SSL 端口（可选） |
| `smtpUsername` / `smtpPassword` | `""` | SMTP 认证（可选） |
| `filesizeChunkingCutoffMB` | `"10"` | 分块发布的文件大小阈值 |
| `numRowsPerChunk` | `"50000"` | 每块行数 |

### 3.2 运行时发布链路（每次 ETL 执行）

```
Shell 启动脚本 (Standard_ETL.sh)
    |
    v
ETL_Setup.sh  (设置 NOW, PATH, ETL_ROOT_DIR)
    |
    v
Setup-Script.sh  (调用 kitchen.sh)
    |
    v
Standard_ETL.kjb  (主编排作业)
    |-- Set variables  (Data_Sync_ETL, ETL_Transformation_Name, ETL_Specific_To_Addresses)
    |-- ETL_Specific_To_Addresses  (读取额外邮件收件人)
    |-- Run KTR  (执行具体 ETL 转换，如基于 ETL_Template.ktr 创建的转换)
    |       |
    |       v
    |   Table input  (SQL 查询源数据)
    |       |
    |       v
    |   Get Variables  (解析 File/DatasetID/ControlFile 路径)
    |       |
    |       v
    |   Prepare_for_DataSyncSSync/FTP/HTTP (子转换)
    |       |-- OS 检测: Windows=剥 8 字符, Linux/Mac=剥 7 字符
    |       |-- 公式: Right([File]; Len([File]) - [CharactersToStrip])
    |       |-- 剥离 file:// 或 file:/// URI 前缀
    |       |
    |       v
    |   Text file output  (写出 CSV 文件)
    |       |
    |       v
    |   Wait for text file  (阻塞等待文件写出完成)
    |       |
    |       v
    |   DataSyncHTTP/FTP/DataSync (子转换)
    |       |-- GetVariable: DATA_SYNC_DIRECTORY
    |       |-- Build DataSync Command (Formula 步骤，拼接命令行)
    |       |-- Run DataSync Job (ExecProcess 步骤，执行 Java 命令)
    |       |-- Write to log (记录 DataSyncResultOutput/ErrorOutput/ExitValue)
    |       |
    |       v
    |   DSResult (JavaScript 格式化结果)
    |       |
    |       v
    |   Copy rows to result  (传递给父作业)
    |
    |-- [成功] --> ETL_Completion_E-Mail - TRUE
    |-- [失败] --> ETL_Completion_E-Mail - FALSE
```

### 3.3 三种 DataSync 变体对比

| 特性 | `DataSync.ktr` (通用/SODA) | `DataSyncFTP.ktr` (FTP) | `DataSyncHTTP.ktr` (HTTP) |
|---|---|---|---|
| **输入字段** | `file`, `header`, `datasetid`, `method` | `file`, `datasetid`, `controlfile` | `file`, `datasetid`, `controlfile` |
| **发布方法** | 由调用者通过 `method` 字段指定 | 硬编码 `replace` | 硬编码 `replace` |
| **发布协议** | 默认 SODA API (HTTP) | FTP (`--publishViaFTP true`) | HTTP (`--publishViaHttp true`) |
| **控制文件** | 无 | `--pathToFTPControlFile` | `--pathToControlFile` |
| **表头行** | `--fileToPublishHasHeaderRow` 参数 | 无此参数 | 无此参数 |
| **JVM 堆内存** | 默认 | 默认 | `-Xmx1g` (1 GB) |
| **凭据传递** | 不传（使用 Java Preferences） | 不传（使用 Java Preferences） | 不传（使用 Java Preferences） |
| **对应 Prepare 转换** | 无 | `Prepare_for_DataSyncFTP.ktr` | `Prepare_for_DataSyncHTTP.ktr` |

**拼装的命令行示例**:

- **DataSync.ktr**:
  ```
  java -jar <DataSyncDir>datasync.jar --fileToPublish <file> --fileToPublishHasHeaderRow <header> --datasetID <datasetid> --publishMethod <method>
  ```

- **DataSyncFTP.ktr**:
  ```
  java -jar <DataSyncDir>datasync.jar --fileToPublish <file> --datasetID <datasetid> --publishMethod replace --publishViaFTP true --pathToFTPControlFile <controlfile>
  ```

- **DataSyncHTTP.ktr**:
  ```
  java -jar -Xmx1g <DataSyncDir>datasync.jar --fileToPublish <file> --datasetID <datasetid> --publishMethod replace --publishViaHttp true --pathToControlFile <controlfile>
  ```

### 3.4 Prepare 路径预处理子转换

Kettle 内部使用 `file://`（Linux/Mac，7 字符）或 `file:///`（Windows，8 字符）格式的 URI 路径，而 DataSync JAR 需要操作系统原生路径。因此需要 `Prepare_for_DataSync*.ktr` 系列转换来剥离前缀。

处理流程:
1. **Mapping input** -- 接收 `File` 和 `ControlFile`（HTTP 变体仅接收 `File`）
2. **DetectLastRow** -- 标记最后一行
3. **FilterRows** -- 仅保留最后一行（安全防护）
4. **GetVariable** -- 获取 `${os.name}`
5. **JavaScript** -- 根据 OS 类型决定剥离字符数（Windows=8, Linux=7, Mac=7, Unknown=7/8）
6. **Formula** -- `Right([File]; Len([File]) - [CharactersToStrip])` 执行剥离
7. **Mapping output** -- 输出清洗后的路径

### 3.5 控制文件 (_control.json)

DataSync FTP/HTTP 变体使用控制文件来定义 CSV 解析规则和发布行为。模板 `ETL/_control.json` 示例:

```json
{
  "action": "Replace",
  "csv": {
    "fixedTimestampFormat": "MM/dd/yyyy",
    "separator": ",",
    "timezone": "UTC",
    "skip": 0,
    "encoding": "utf-8",
    "trimWhitespace": true,
    "quote": "\"",
    "emptyTextIsNull": true,
    "columns": ["api_name_1", "api_name_2", "api_name_3"],
    "trimServerWhitespace": true
  }
}
```

每个 ETL 需要创建自己的 `_control.json`，把 `columns` 数组改为实际的 Socrata API 字段名。

---

## 四、Kettle Properties 环境变量依赖

所有 DataSync 子转换的 notepad 中均注明，`kettle.properties` 文件（位于 `$HOME/.kettle/kettle.properties`）中必须配置以下变量:

| 变量名 | 用途 | 示例值 |
|---|---|---|
| `ETL_DIRECTORY` | ETL 框架根目录的绝对路径，以 `/` 结尾 | `/opt/open-data-etl-utility-kit/ETL/` |
| `DATA_SYNC_DIRECTORY` | DataSync 安装目录的绝对路径，以 `/` 结尾 | `/opt/open-data-etl-utility-kit/DataSync/` |

此外，`ETL/ETL_Setup.sh` 中还硬编码了以下需要用户修改的路径:

| 变量 | 位置 | 说明 |
|---|---|---|
| `ETL_ROOT_DIR` | `ETL/ETL_Setup.sh` 第 3 行 | 框架 ETL 子目录的绝对路径 |
| `PATH` (追加 JDK) | `ETL/ETL_Setup.sh` 第 2 行 | JDK bin 目录路径 |

---

## 五、面向代码评审的验证说明

### 5.1 配置项检查清单

| # | 检查项 | 文件/位置 | 验证方法 |
|---|---|---|---|
| 1 | `config_template.json` 中所有占位符值是否已替换 | `DataSync/config_template.json` | 人工确认 `domain`、`username`（含防锁号括号需移除）、`password`、`appToken`、`adminEmail`、`logDatasetID`、SMTP 相关字段 |
| 2 | `kettle.properties` 中 `ETL_DIRECTORY` 和 `DATA_SYNC_DIRECTORY` 是否配置正确 | `$HOME/.kettle/kettle.properties` | 确认路径存在且以 `/` 结尾 |
| 3 | `ETL_Setup.sh` 中的 `/path/to/` 占位符是否已替换 | `ETL/ETL_Setup.sh` 第 2-3 行 | 检查 `PATH` 和 `ETL_ROOT_DIR` 是否为实际路径 |
| 4 | `Standard_ETL.sh` 中的 ETL_NAME 和 ETL_DIR_RELATIVE 是否已配置 | `ETL/Standard_ETL.sh` 第 3-5 行 | 检查模板占位符是否已替换为真实 ETL 名称和目录 |
| 5 | `datasync.jar` 是否已从版本化文件重命名 | `DataSync/datasync.jar` | 确认文件存在（`.gitignore` 排除了此文件） |
| 6 | 各 ETL 的 `_control.json` 中 `columns` 数组是否与实际 Socrata 数据集字段匹配 | 各 ETL 目录下的 `*_control.json` | 对照 Socrata 门户上数据集的 API 字段名 |
| 7 | 各 ETL 的 `P_DatasetID` 参数是否指向正确的 Socrata 4x4 数据集 ID | 各 ETL `.ktr` 的参数配置 | 在 Socrata 门户上验证 4x4 ID 存在 |

### 5.2 文件路径检查清单

| # | 检查项 | 依赖关系 |
|---|---|---|
| 1 | `${DATA_SYNC_DIRECTORY}datasync.jar` 路径是否可访问 | 所有 `DataSync*.ktr` 的 `DataSync Directory` 步骤拼接路径 |
| 2 | `${ETL_DIRECTORY}Utilities/DataSyncHTTP.ktr` 等子转换文件是否存在 | `ETL_Template.ktr` 中 Mapping 步骤的 `filename` 引用 |
| 3 | `${ETL_DIRECTORY}Utilities/Prepare_for_DataSyncSSync.ktr` 等预处理转换是否存在 | 同上 |
| 4 | `${ETL_DIRECTORY}Utilities/ETL_Completion_E-Mail.ktr` 是否存在 | `Standard_ETL.kjb` 中邮件通知步骤 |
| 5 | `${ETL_DIRECTORY}Utilities/ETL_Specific_To_Addresses.ktr` 是否存在 | `Standard_ETL.kjb` 中收件人加载步骤 |
| 6 | `${Internal.Transformation.Filename.Directory}/files/` 目录是否存在 | `ETL_Template.ktr` 中 CSV 输出路径 |
| 7 | `${Internal.Transformation.Filename.Directory}/ETL_Specific_To_Addresses.txt` 是否存在 | `ETL_Specific_To_Addresses.ktr` 中读取的收件人列表文件 |
| 8 | `../data-integration/kitchen.sh` 是否可从 ETL 目录访问 | `Setup-Script.sh` 调用 Kettle 引擎 |
| 9 | `../Log/` 目录是否存在且可写 | `Setup-Script.sh` 日志输出 |

### 5.3 外部凭据与运行时依赖

| # | 依赖项 | 类型 | 说明 |
|---|---|---|---|
| 1 | **Socrata 账户凭据** | 凭据 | `username` + `password`，通过 `load_preferences.sh` 一次性写入 DataSync Java Preferences；**运行时不在命令行或配置文件中传递** |
| 2 | **Socrata App Token** | API 令牌 | 同上，通过 config.json 加载到 Preferences |
| 3 | **Socrata 数据集权限** | 权限 | 运行 DataSync 的用户必须对目标 `datasetid` 拥有编辑权限 |
| 4 | **JDK/JRE** | 运行时 | `java` 命令必须可用（`ETL_Setup.sh` 中追加 PATH，或直接通过系统 PATH） |
| 5 | **Pentaho/Kettle** | 运行时 | `kitchen.sh` / `spoon.sh` 需安装在 `data-integration/` 目录下 |
| 6 | **SMTP 邮件服务器** | 网络服务 | `ETL_Completion_E-Mail.ktr` 需要可达的 SMTP 服务器发送通知邮件 |
| 7 | **源数据库连接** | 数据库 | 各 ETL 的 `Table input` 步骤需要已配置的 Kettle 数据库连接 |
| 8 | **网络连通性** | 网络 | 运行机器需能访问 Socrata 门户域名（HTTPS/FTP） |

### 5.4 已知风险与注意事项

| # | 风险点 | 文件/位置 | 说明 |
|---|---|---|---|
| 1 | **config.json 残留** | `DataSync/load_preferences.sh` 注释 | 脚本注释明确要求运行后删除 `config.json`（含明文密码），但未自动化清理 |
| 2 | **模板中的加密密码** | `ETL_Template.ktr` 第 76 行 | `<slaveserver>` 元素包含 `Encrypted 2be98afc86af4858da51efc43db868af6`，这是 Kettle 内部加密的密码残留，评审时需确认此 slave server 配置是否仍需要 |
| 3 | **ETL_Completion_E-Mail.ktr 中的硬编码加密密码** | `ETL/Utilities/ETL_Completion_E-Mail.ktr` | Mail 步骤的 `<auth_password>` 字段包含加密密码，评审时需确认 |
| 4 | **OS 路径剥离的容错性** | `Prepare_for_DataSync*.ktr` 中的 JavaScript | 当 OS 类型未知时使用默认值（7 或 8），可能导致路径错误；且 `Prepare_for_DataSyncHTTP.ktr` 和 `Prepare_for_DataSyncFTP.ktr` 的 Unknown 默认值不一致（8 vs 7） |
| 5 | **DataSync 版本锁定** | `DataSync/DataSync-1.5.3.jar` | 框架绑定 v1.5.3，升级需手动替换 JAR 并重命名为 `datasync.jar` |
| 6 | **`failwhennotsuccess=N`** | 所有 `DataSync*.ktr` 的 `Run DataSync Job` 步骤 | ExecProcess 步骤设 `failwhennotsuccess=N`，即使 DataSync 返回非零退出码，Kettle 步骤本身仍标记为成功；错误仅在日志和结果字段中可见 |
| 7 | **`Standard_ETL.sh` 使用 source 执行** | `ETL/Standard_ETL.sh` 第 9 行 | 使用 `. $ETL_ROOT_DIR/Standard_ETL.sh`（source），如果 Setup-Script.sh 失败，不会中断外层脚本 |

---

## 六、静态验证方法（不修改代码）

以下方法可在不运行 ETL 的前提下验证配置正确性:

### 6.1 文件存在性验证

```bash
# 验证 DataSync JAR 是否存在
test -f DataSync/datasync.jar && echo "OK" || echo "MISSING"

# 验证所有子转换文件是否存在
for f in DataSync DataSyncFTP DataSyncHTTP \
         Prepare_for_DataSyncFTP Prepare_for_DataSyncHTTP Prepare_for_DataSyncSSync \
         ETL_Completion_E-Mail ETL_Specific_To_Addresses; do
  test -f "ETL/Utilities/${f}.ktr" && echo "${f}.ktr OK" || echo "${f}.ktr MISSING"
done

# 验证 kettle.properties 中的路径变量
grep -E "^ETL_DIRECTORY|^DATA_SYNC_DIRECTORY" ~/.kettle/kettle.properties
```

### 6.2 Kettle 变量引用验证

在 Kettle Spoon GUI 中:
1. 打开 `Standard_ETL.kjb`
2. 使用 "Edit > Settings" 检查参数 `P_Data_Sync_ETL`、`P_ETL_Transformation_Directory`、`P_ETL_Transformation_Name`
3. 使用 Tools > Repository > Explore 检查所有 `${ETL_DIRECTORY}` 和 `${DATA_SYNC_DIRECTORY}` 引用是否可解析

### 6.3 DataSync 凭据验证

```bash
# 在 DataSync 目录运行（需要先完成 load_preferences）
cd DataSync/
java -jar datasync.jar --jobType testConnection
# 如果凭据已正确加载到 Java Preferences 中，此命令应返回连接成功
```

### 6.4 控制文件 JSON 语法验证

```bash
# 验证 _control.json 语法（需要 jq 或 python）
python -c "import json; json.load(open('ETL/_control.json'))" && echo "Valid JSON" || echo "Invalid JSON"
```

### 6.5 Shell 脚本路径验证

```bash
# 验证 ETL_Setup.sh 中的路径是否有效
source ETL/ETL_Setup.sh 2>/dev/null
echo "ETL_ROOT_DIR=$ETL_ROOT_DIR"
test -d "$ETL_ROOT_DIR" && echo "ETL_ROOT_DIR exists" || echo "ETL_ROOT_DIR NOT FOUND"
```

---

## 七、总结

本项目的 Kettle ETL 与 Socrata DataSync 衔接可概括为三层架构:

1. **凭据层**（部署时）: `config_template.json` -> `config.json` -> `load_preferences.sh` -> Java Preferences Store
2. **路径适配层**（运行时预处理）: `Prepare_for_DataSync*.ktr` 系列负责 OS 相关的文件路径清洗
3. **发布执行层**（运行时核心）: `DataSync.ktr` / `DataSyncFTP.ktr` / `DataSyncHTTP.ktr` 拼装命令行并通过 `ExecProcess` 步骤执行 `datasync.jar`

主编排作业 `Standard_ETL.kjb` 将以上三层串联为完整的 ETL 生命周期: 变量初始化 -> 收件人加载 -> ETL 转换执行（含数据抽取/转换/文件输出/DataSync 发布）-> 邮件通知。

评审重点应集中在: 凭据安全（config.json 清理、模板中残留的加密密码）、路径变量一致性（`ETL_DIRECTORY` / `DATA_SYNC_DIRECTORY` 配置）、OS 路径剥离逻辑的健壮性、以及 DataSync 非零退出码在 `failwhennotsuccess=N` 设置下的错误传播行为。
