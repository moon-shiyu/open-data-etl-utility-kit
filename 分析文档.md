# ETL 日志诊断工具 — 代码解析文档

## 1. 项目背景

本仓库（open-data-etl-utility-kit）是一套面向政府开放数据平台的 ETL 工具集，核心使用 Pentaho/Kettle 数据集成软件将数据从数据库同步至 Socrata 数据门户。ETL 作业通过 crontab 定时调度，每次运行会产生带时间戳的日志文件。`Log/` 目录下存放了四个诊断脚本，帮助管理员快速排查 ETL 运行状态。

### 1.1 日志文件命名规则

日志文件由 `Standard_ETL.sh` 中的 Kettle `kitchen.sh` 生成，命名格式为：

```
{ETL_NAME}_{YYYYMMDD}_{HHMMSS}.txt
```

例如：`Data_Set_Name_abcd-1234_20260615_030000.txt`

日志内容包含 Kettle 的 Detailed 级别输出，其中关键行含有 `"Processing ended after"` 字样，记录作业总耗时。

### 1.2 crontab 调度格式

ETL 调度条目示例：

```
* * * * * /path/to/abcd-1234.sh >> /path/to/abcd-1234.log 2>&1
```

前 5 个字段为 cron 时间表达式，第 6 个字段起为实际执行的命令。

---

## 2. 四份脚本逐一解析

### 2.1 `A_DatasetLogs.sh` — 数据集日志查找

**源码：**

```bash
if [ -z "$1" ]
then
   echo "Usage: A_DatasetLogs.sh StringToFindInLogName"
   exit 1
else
   ls -l | grep $1
   exit 0
fi
```

**功能说明：**

| 项目 | 内容 |
|------|------|
| 输入参数 | `$1` — 数据集名称或 4x4 标识中的任意子串 |
| 核心逻辑 | `ls -l \| grep $1` — 列出当前目录（即 Log 目录）所有文件，过滤含参数名的条目 |
| 输出 | 匹配日志文件的完整 `ls -l` 行（权限、大小、日期、文件名） |

**排查场景：**
- 管理员想确认某个数据集**是否有日志产生**、**产生了多少份**、**最近一份是什么时间**。
- 通过文件大小可以初步判断运行是否正常（大小为 0 或异常小可能表示 ETL 未成功执行）。

**典型调用：**

```bash
sh Log/A_DatasetLogs.sh abcd-1234
```

---

### 2.2 `A_ETLRuntimes.sh` — ETL 运行耗时统计

**源码：**

```bash
if [ -z "$1" ]
then
   echo "Usage: A_ETLRuntimes.sh StringToFindInLogName"
   exit 1
else
   cat *$1* | grep 'Processing ended after'
   exit 0
fi
```

**功能说明：**

| 项目 | 内容 |
|------|------|
| 输入参数 | `$1` — 数据集名称或 4x4 标识 |
| 核心逻辑 | `cat *$1*` — 通配符展开所有匹配文件名的日志；`grep 'Processing ended after'` — 提取运行耗时行 |
| 输出 | 每次 ETL 运行的 "Processing ended after ..." 行 |

**排查场景：**
- 管理员需要**追踪某个数据集历史运行时间趋势**，发现耗时突增可能指示上游数据库性能问题或网络延迟。
- 多次运行的结果对比，可识别间歇性超时。

**典型调用：**

```bash
sh Log/A_ETLRuntimes.sh abcd-1234
# 输出示例：
# Processing ended after 00:03:42.567
# Processing ended after 00:03:38.123
# Processing ended after 00:15:01.890   ← 异常耗时
```

---

### 2.3 `A_TodayLogs.sh` — 当天日志总览

**源码：**

```bash
DATASETS_TO_EXCLUDE="n4j6-wkkf|t2qc-9pjd|qmqz-2xku|97wa-y6ff"

if [ -z "$1" ]
then
   ls -l | grep $(date +"%Y%m%d")
   exit 0
else
   case $1 in
      -e)
         ls -l | grep $(date +"%Y%m%d") | grep -Ev $DATASETS_TO_EXCLUDE
         exit 0
         ;;
      *)
         echo "Usage: A_Today.sh [-e]"
         echo "   -e Exclude a pre-specified set of datasets by 4x4..."
         exit 1
         ;;
   esac
fi
```

**功能说明：**

| 项目 | 内容 |
|------|------|
| 输入参数 | 无参数 = 列出今天所有日志；`-e` = 排除高频数据集 |
| 核心逻辑 | `grep $(date +"%Y%m%d")` 匹配文件名中的日期；`grep -Ev` 以扩展正则排除指定 4x4 |
| 排除列表 | `DATASETS_TO_EXCLUDE` 用管道符 `\|` 分隔多个 4x4，作为硬编码配置 |

**排查场景：**
- 管理员每天早晨**巡检当日全部 ETL 执行情况**，快速发现哪些数据集未产生日志（即未运行）。
- `-e` 模式排除高频数据集（如每 15 分钟运行一次的实时数据集），减少输出噪声。

**典型调用：**

```bash
# 查看全部今日日志
sh Log/A_TodayLogs.sh

# 排除高频数据集后查看
sh Log/A_TodayLogs.sh -e
```

---

### 2.4 `A_RunETL.sh` — 手动触发 ETL

**源码：**

```bash
if [ -z "$1" ]
then
   echo "Usage: A_RunETL.sh Dataset4x4"
   exit 1
else
   if [ -z $(echo "$1" | grep "^[a-zA-Z0-9]\{4\}-[a-zA-Z0-9]\{4\}$") ]; then
      echo "Invalid Dataset4x4"
      exit 1
   fi
   CRONTAB_LINE="crontab -l | grep $1 | cut -d ' ' -f 6-"
   COMMAND_TO_RUN=$(eval $CRONTAB_LINE)
   eval $COMMAND_TO_RUN
   echo "Command executed: " $COMMAND_TO_RUN
   exit 0
fi
```

**功能说明：**

| 项目 | 内容 |
|------|------|
| 输入参数 | `$1` — 严格格式的 4x4 标识（如 `abcd-1234`） |
| 参数校验 | `grep "^[a-zA-Z0-9]\{4\}-[a-zA-Z0-9]\{4\}$"` — 正则强制校验 4 字符-4 字符格式 |
| 核心逻辑 | ① `crontab -l \| grep $1` 找到包含该 4x4 的 crontab 行；② `cut -d ' ' -f 6-` 截取第 6 字段起的内容（跳过 5 个 cron 时间字段）；③ `eval` 执行提取出的命令 |
| 输出 | 打印实际执行的命令供确认 |

**排查场景：**
- 管理员发现某个数据集**当天未成功运行**，需要**立即手动触发一次**而不必等待 cron 调度。
- 通过复用 crontab 中的命令，确保手动执行与定时执行的参数完全一致，避免人为配置差异。

**典型调用：**

```bash
sh Log/A_RunETL.sh abcd-1234
# 输出：Command executed: /path/to/abcd-1234.sh >> /path/to/abcd-1234.log 2>&1
```

---

## 3. 四份脚本的协作关系

```
                    ┌──────────────────────────────────┐
                    │          管理员日常排查流程          │
                    └───────────────┬──────────────────┘
                                    │
                     ┌──────────────▼──────────────┐
                     │  A_TodayLogs.sh             │
                     │  第一步：查看今天全部日志       │
                     │  发现缺失或异常的数据集         │
                     └──────────────┬──────────────┘
                                    │
                     ┌──────────────▼──────────────┐
                     │  A_DatasetLogs.sh <4x4>     │
                     │  第二步：查看该数据集全部日志   │
                     │  确认日志文件数量/大小/时间     │
                     └──────────────┬──────────────┘
                                    │
                     ┌──────────────▼──────────────┐
                     │  A_ETLRuntimes.sh <4x4>     │
                     │  第三步：分析历史运行耗时      │
                     │  定位性能退化或超时            │
                     └──────────────┬──────────────┘
                                    │
                     ┌──────────────▼──────────────┐
                     │  A_RunETL.sh <4x4>          │
                     │  第四步：手动触发一次 ETL      │
                     │  用于紧急恢复或验证修复         │
                     └─────────────────────────────┘
```

---

## 4. 关键配置与链路说明

| 配置项 | 位置 | 说明 |
|--------|------|------|
| `DATASETS_TO_EXCLUDE` | `A_TodayLogs.sh` 第 2 行 | 管道符分隔的 4x4 列表，需要管理员按实际高频数据集维护 |
| crontab 调度条目 | 系统 `crontab -l` | 前 5 字段为时间，第 6 字段起为命令；`A_RunETL.sh` 依赖此格式 |
| `ETL_Setup.sh` | `ETL/` 目录 | 定义 `NOW`、`PATH`、`ETL_ROOT_DIR`，所有 ETL 脚本的公共前置配置 |
| 日志输出路径 | `Standard_ETL.sh` 第 7 行 | `> "../Log/${ETL_NAME}_${NOW}.txt"` 决定了日志文件的存放位置和命名 |
| Kettle 日志级别 | `Standard_ETL.sh` 第 7 行 | `-level=Detailed` 确保输出包含 `Processing ended after` 行 |

---

## 5. 静态验证方法（无生产日志环境）

在没有真实 ETL 日志文件和 crontab 条目的开发/测试环境中，可以通过以下方法对脚本逻辑进行静态验证。

### 5.1 创建模拟日志文件

```bash
cd Log/

# 模拟多个数据集的日志文件（符合命名规则）
touch "Data_Set_A_abcd-1234_20260615_030000.txt"
touch "Data_Set_A_abcd-1234_20260614_030000.txt"
touch "Data_Set_B_efgh-5678_20260615_060000.txt"
touch "Data_Set_C_n4j6-wkkf_20260615_001500.txt"   # 高频数据集

# 写入模拟耗时行
echo "Processing ended after 00:03:42.567" > "Data_Set_A_abcd-1234_20260615_030000.txt"
echo "Processing ended after 00:04:01.234" > "Data_Set_A_abcd-1234_20260614_030000.txt"
echo "Processing ended after 00:02:15.890" > "Data_Set_B_efgh-5678_20260615_060000.txt"
echo "Processing ended after 00:00:30.000" > "Data_Set_C_n4j6-wkkf_20260615_001500.txt"
```

### 5.2 验证 `A_DatasetLogs.sh`

```bash
# 测试：按 4x4 过滤
sh Log/A_DatasetLogs.sh abcd-1234
# 期望输出：包含两条 abcd-1234 日志文件的 ls -l 行

# 测试：无参数
sh Log/A_DatasetLogs.sh
# 期望输出：Usage 提示信息

# 测试：不存在的名称
sh Log/A_DatasetLogs.sh zzzz-0000
# 期望输出：无匹配，退出码 0（注意：此处无错误提示）
```

### 5.3 验证 `A_ETLRuntimes.sh`

```bash
# 测试：提取耗时
sh Log/A_ETLRuntimes.sh abcd-1234
# 期望输出：
# Processing ended after 00:03:42.567
# Processing ended after 00:04:01.234

# 测试：不存在的名称
sh Log/A_ETLRuntimes.sh zzzz-0000
# 期望输出：cat 报 "No such file" 错误（glob 无匹配时传入字面量）
```

### 5.4 验证 `A_TodayLogs.sh`

```bash
# 测试：查看全部今日日志（仅匹配今天日期的文件）
sh Log/A_TodayLogs.sh
# 期望输出：文件名含 20260615 的条目

# 测试：排除高频数据集
sh Log/A_TodayLogs.sh -e
# 期望输出：排除了 n4j6-wkkf 等 4x4 的今日日志

# 测试：无效参数
sh Log/A_TodayLogs.sh --invalid
# 期望输出：Usage 提示信息

# 技巧：修改系统日期或使用 date -s 来测试不同日期的匹配
# 或直接修改脚本中的 date 命令为固定值进行验证：
#   将 $(date +"%Y%m%d") 临时替换为 20260615
```

### 5.5 验证 `A_RunETL.sh`

由于该脚本依赖 `crontab` 和 `eval`，需分阶段验证：

```bash
# 阶段 1：参数校验
sh Log/A_RunETL.sh
# 期望：Usage 提示

sh Log/A_RunETL.sh "invalid"
# 期望：Invalid Dataset4x4

sh Log/A_RunETL.sh "ab-12"
# 期望：Invalid Dataset4x4

sh Log/A_RunETL.sh "abcd-1234"
# 期望：格式校验通过（后续因无 crontab 条目会失败）

# 阶段 2：模拟 crontab 条目
# 在测试环境中添加 crontab 行：
# crontab -e
# 0 3 * * * /path/to/abcd-1234.sh >> /path/to/abcd-1234.log 2>&1

# 阶段 3：仅验证 crontab 解析逻辑（不实际执行）
# 用 echo 替换 eval 执行：
echo "crontab -l | grep abcd-1234 | cut -d ' ' -f 6-" | sh
# 检查输出是否为预期的命令字符串
```

### 5.6 grep / cut / eval 关键用法说明

| 工具 | 脚本 | 用法 | 说明 |
|------|------|------|------|
| `grep` | `A_DatasetLogs.sh` | `ls -l \| grep $1` | 按子串匹配文件名 |
| `grep` | `A_TodayLogs.sh` | `grep $(date +"%Y%m%d")` | 匹配当前日期字符串 |
| `grep -Ev` | `A_TodayLogs.sh` | `grep -Ev $DATASETS_TO_EXCLUDE` | `-E` 扩展正则，`-v` 反向匹配（排除） |
| `grep` | `A_RunETL.sh` | `grep "^[a-zA-Z0-9]\{4\}-...\{4\}$"` | BRE 正则严格校验 4x4 格式 |
| `grep` | `A_ETLRuntimes.sh` | `grep 'Processing ended after'` | 精确匹配 Kettle 耗时行 |
| `cut` | `A_RunETL.sh` | `cut -d ' ' -f 6-` | 以空格分隔，取第 6 字段至末尾（跳过 cron 时间） |
| `eval` | `A_RunETL.sh` | `eval $CRONTAB_LINE` → `eval $COMMAND_TO_RUN` | 两次 eval：第一次解析命令字符串，第二次执行命令 |
| `cat *$1*` | `A_ETLRuntimes.sh` | `cat *$1*` | Shell glob 展开匹配多个文件后合并输出 |

---

## 6. 外部环境依赖

| 依赖项 | 用途 | 影响范围 |
|--------|------|----------|
| **crontab** | `A_RunETL.sh` 通过 `crontab -l` 读取调度命令 | 仅 `A_RunETL.sh`；无 crontab 则该脚本不可用 |
| **Kettle kitchen.sh** | `Standard_ETL.sh` 调用 Kettle 执行 ETL 并生成日志 | 日志文件的生成源头；无 Kettle 则无日志 |
| **date 命令** | `A_TodayLogs.sh` 和 `ETL_Setup.sh` 使用 `date` 获取时间戳 | 跨平台差异：macOS/Linux 的 `date` 行为略有不同 |
| **Log 目录为工作目录** | `A_DatasetLogs.sh`、`A_ETLRuntimes.sh`、`A_TodayLogs.sh` 均依赖在 Log 目录下执行 `ls`/`cat` | 需要在 Log 目录下运行，或通过 `cd Log/` 切换后再执行 |
| **Java 1.6+** | Kettle 运行时依赖 | 间接依赖，影响日志生成 |
| **DataSync** | Socrata 数据同步工具 | 间接依赖，影响 ETL 执行但不影响日志诊断脚本 |

---

## 7. 已知局限性与注意事项

1. **`A_DatasetLogs.sh` 和 `A_ETLRuntimes.sh` 的参数未加引号**：`grep $1` 未使用 `"$1"`，如果参数含空格或特殊字符可能导致意外行为。
2. **`A_ETLRuntimes.sh` 的 glob 无匹配时**：`cat *$1*` 在无匹配文件时，bash 默认将字面量传给 `cat`，会报 "No such file" 错误而非友好提示。
3. **`A_RunETL.sh` 的双重 eval**：两次 `eval` 存在命令注入风险——如果 crontab 内容被恶意篡改，eval 会直接执行。此外如果 crontab 中该 4x4 出现多行（如注释行），`COMMAND_TO_RUN` 会包含多行命令。
4. **`A_TodayLogs.sh` 的排除列表硬编码**：`DATASETS_TO_EXCLUDE` 写死在脚本中，新增高频数据集需要手动编辑脚本。
5. **工作目录依赖**：三个日志查看脚本必须在 Log 目录下执行（`cd Log/ && sh A_DatasetLogs.sh ...`），因为它们使用 `ls -l` 和 `cat *$1*` 操作当前目录的文件。
